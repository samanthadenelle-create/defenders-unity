# Release work log — 2026-09-11

## Completed

- Added the catalog-wide fresh-town building visual regression. It checks every Addressables-backed base visual and authored upgrade tier for a renderable mesh, material/shader presence, ground seating, and fit bounds.
- Registered that gate in `DataRegression`. Evidence: `Builds/fresh-town-building-visual.log` (`FRESH_TOWN_BUILDING_VISUAL_OK 35`) and `Builds/fresh-regression-after-building-gate.log` (`REGRESSION_OK 513/513 suites`).
- Fixed the first-run localization keys for the offline setup dialog in Resources and StreamingAssets for all supported locales. Rebuilt localization tables and passed `LOCALIZATION_REGRESSION_OK`.
- Fixed forced structure albedo binding to use the project albedo-slot oracle, including Synty `_Albedo_Map` shaders. Full data regression passed after the change.
- Built the Windows development EXE with the current code: `Builds/Windows/DefendersOfTheRealm.exe`.
- Ran the approved parent `ServerData` R2 ship path. Final parity: `R2_PARITY_OK targets=Android,StandaloneWindows64,WebGL objects=225`.
- Built the Android APK and distributed it through Firebase App Distribution to the `testers` group. Release: `2026.09.12.365875`.
- Captured and verified the live EXE reaches the castle with real structure bundles resolving. The confirmed Arcane Tower shader mismatch is now addressed in source.

## Outstanding

- WebGL build did not produce `Builds/WebGL/index.html` in either attempt; the Unity process stopped during the script-compilation handoff. No Vercel deployment was made.
- The Android AAB was not built or uploaded in this sequence; only the APK was distributed.
- A fresh post-fix EXE visual capture after dismissing the daily chest modal is still needed to visually confirm the Arcane Tower color fix in scene.
- The worktree contains many pre-existing changes outside this release slice. They remain unstaged for review and must not be folded into a release commit without separate verification.

## Evidence files

- `Builds/fresh-town-building-visual.log`
- `Builds/fresh-regression-after-building-gate.log`
- `Builds/exe-localization-regression.log`
- `Builds/arcane-binder-regression`
- `Builds/android-build.log`
- `Builds/r2-parity.log`
- `Builds/exe-window-final.png`
