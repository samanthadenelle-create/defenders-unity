# Save-first migration pointer

Work order: `WorkOrders/WORK_ORDER_FOOD_TO_STONE_2026-09-11.md`.

## Current slice

`Assets/_Modules/Core/State/NestedTypes.cs` ResourceBalance: runtime storage renamed to Stone; JSON name remains food; Unity old-field alias supplied. The temporary Food runtime property has now been removed; only explicit serialization compatibility names remain on the wallet.

`Assets/_Modules/Core/State/SaveSchema.cs` resource validation now operates on Stone while retaining the exact legacy wire diagnostic path.

`Assets/_Modules/Core/State/GameStateService.cs` local/cloud legacy top-level alias assignment and time-derived balance read/write now use Stone.

## Next pointer after verification

1. DONE, terminal PASS `Builds/stone-save-callers.log`: GameStateService.BuildDeltaPayload and ResourcesDiffer read Stone. SyncDeltaPayload.Stone preserves the PascalCase `Food` JSON key required by old offline queues/backend `f.Food`. Old queue deserialization and outgoing key shape tested.
2. DONE in the same passing slice: GameStateService.AddStone replaces AddFood; all five production call sites migrated (two EconomyService calls, raid victory, raid deploy payout and daily quest reward). No AddFood method/call remains in Assets C#; source-based regression expectations updated to the real method name. Spending, notification and save semantics unchanged.
3. DONE, terminal PASS `Builds/stone-save-baskets.log` at 13:03:52, zero compiler errors: queue paid baskets now use BuildJobData.PaidStone and JobCost.Stone, including BuildTimerService cancellation credit. Original `paidFood` and JobCost `Food` JSON keys remain compatibility boundaries, with Unity aliases for renamed fields. No runtime alias added for these types. SaveSchema validation and queue regression references migrated. Focused proof verifies legacy paid amounts, outgoing key shape, zero unrecorded refund and Stone description. The full queue runtime suite still needs a fresh run; do not infer end-to-end cancellation validation from the DTO proof.
4. NEXT: SaveSchema nested dungeon loot. LootStash has separate historical Food and Stone fields. Current scan finds only DTO construction in TestSupport and save validation; no production reward caller using this type. Preserve both historical fields until their contract is reviewed, rather than silently summing or dropping values. This type must not be mistaken for the current wallet or used to reintroduce Food payouts.
5. IMPLEMENTED: Core catalog ResourceCost.stone replaces food, with JsonProperty("food") and FormerlySerializedAs("food") for existing payloads/assets. Migrated the 26 caller files discovered by field access, spanning town repair allowance/proposals, build/upgrade costs, repair quotes, refunds, cost cards and regressions. No source alias on this type. Separate tuple fields and wire strings were excluded. Focused serialization proof passed `Builds/stone-catalog-costs.log`. Integrated owned-town, queue and build-economy proof terminal PASS at 13:08:58: STONE_ECONOMY_INTEGRATION_OK in `Builds/stone-economy-integration-retry.log`, zero compiler errors. Initial integration runner had a namespace qualification error, corrected before this passing rerun. The owned-town suite now explicitly tests Stone shortage refusal and exact deduction without mutating the original state.
6. Backend api/game/save.js guarded/nested/time-derived balances, legacy delta normalization and api/game/load.js field projection. Design a versioned protocol migration and old-client compatibility tests before emitting a new wire key.
7. Cloud snapshot comparisons/signing, backend guards and Unity serialized data: verify no duplicate balance or changed reconciliation authority, then remove the ResourceBalance.Food bridge when all callers have moved.

## Proof notes

Terminal PASS 2026-09-11 12:59: `Builds/stone-save-migration-retry.log`, `STONE_SAVE_MIGRATION_OK`, zero compiler errors. Real Unity asset preserved all three wallet balances; temporary fixture was deleted by the proof. Core save contract passed. This validates the first slice, not all callers or the future wire rename.

Initial proof passed Newtonsoft old-key loading and repeated balance round trips, then failed on `JsonUtility.FromJson` honoring FormerlySerializedAs. That API is not the production signed-save serializer. Revised proof exercises a real temporary Unity serialized GameState asset with the old field name. Preserve the initial failure as evidence; do not describe all Unity JSON parsers as backward compatible. A focused search found no direct JsonUtility ResourceBalance/GameState serialization calls; that is not a complete indirect-call audit.

## Wallet runtime migration verified — 2026-09-11 13:29

ResourceBalance exposes Stone only. All direct Resources.Food accesses were migrated, then four compiler passes pinpointed 44 additional typed caller tokens. Each pointer is preserved in compiler-wallet-migrations.json. ResourceAuthorityRegression now verifies the actual serialized sync payload retains one Food wire key without emitting a second Stone balance; the runtime economy reads the same Resources.Stone slot as reconciliation. Its other anti-duplication and client/server key checks remain.

Terminal PASS Builds/stone-wallet-runtime-migrated.log / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Covers legacy wallet JSON and actual Unity asset import, absence of a Food runtime member, repeated round trips, offline queue/basket compatibility, core save contract, owned-town Stone repair, queue refunds, build economy and resource authority. Full release gate last ran before the authority fix (507/508 green); no new full pass claimed. Other Food-named resource types, enums, content and protocol boundaries remain open. No build artifacts replaced.
Economy payout expansion terminal PASS at 13:42:20: Builds/stone-economy-payouts.log / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Verified suite set now includes save compatibility/core save, owned town, queue refunds, build economy, resource authority, economy sweep, honest feedback grants and raid scoring. Existing Unity lifecycle exceptions remain. Next migration targets are the remaining enum identities and separate resource/harvest/UI DTOs, followed by reviewed residual inventory and channel compilation. Original release requirements remain open.

## 2026-09-11 13:52 — resource enum migration verified

Renamed runtime enum members to Stone with explicit original numeric values: BankResource=2, ResourceType=2, MineResource=2, HarvestResource=1, HarvestTarget=2, ElarionUiKit.CurrencyKind=4. Each retains EnumMember("Food") so string-enum saves stay readable/writable under the deployed legacy spelling without a Food runtime alias. Qualified references migrated in 25 bank and 41 further enum files; compiler also located four aliased BankResource references in Wallet/PackStore. StorageStackView now uses TownBankCapacity.TryParseResource and the new HarvestResourceNames.TryParse boundary so legacy food/grain and current stone names resolve correctly. Existing Echo assignment storage tokens remain unchanged.

Proof now checks enum numeric identities, absence of Food runtime names, old string-enum round trips and unchanged default numeric serialization; checks legacy/current parser names. Added EchoSpecializationRegression and OfflineHarvestRegression to the integrated suite. Initial proof had a HarvestTarget namespace typo, corrected. BuildEconomyRegression then caught a generated catalog hash mismatch because a qualified enum-name sweep also matched embedded catalog text. Corrected by running the existing CatalogFallbackGenerator.Generate from canonical source, not by editing the checksum. Generator terminal PASS: Builds/stone-enum-catalog-regenerate.log, canonical copies byte-identical, 29 rows/version42/110625 bytes, SHA256 b9e2ae5c7ebdefd98b20486696b8e14431d2d3aa49c49a8d74ce8f2d46b965ab.

Expanded integrated suite terminal PASS: Builds/stone-enums-harvest-validated.log at 13:51:43 / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Includes save, town repair, queue refunds, build economy/catalog parity, authority, economy sweep, feedback grants, raid scoring, Echo specialization and offline harvesting. Existing Unity lifecycle exceptions remain. Other harvest/reward/UI DTO Food members and authored/localized references remain migration work. No new full-release pass or APK/AAB/EXE build claimed.

## 2026-09-11 14:02 — reward, harvest and UI DTOs migrated

Renamed Food members to Stone in fourteen definition files: JewelerRecipeCost, GearRecipeCost, PackEconomy, RewardEconomy, RealmClearReward, DailyQuestVM.RewardInfo, EconomyModel, StakesLedger, WaveClearPayout, OfflineHarvestResult, BuildMenuVM, BuildingUpgradeVM, PartyShopVM and TroopTrainingVM. Existing explicit JSON keys (food/stone/f) retained; formerly unannotated result fields retain capital Food JSON keys via explicit attributes. Mutable serialized fields also carry FormerlySerializedAs("Food"). Pack/season legacy-food decoding remains a compatibility boundary, not another balance. Definition-file private lower-case parameter names remain a later naming cleanup, not a source bridge.

Compiler ledger records 57 further exact caller references over dependency layers (1+5+35+15+1), across grants, crafting, HUD/readouts, offline claim/welcome-back, saved siege loss, tests and UI captures. Legacy field proof covers original JSON key/value loading and round-trip for seven representative DTOs, including read-only wave payout. Expanded integrated suite with PackGrantRegression and SiegeLossStakesRegression. Terminal PASS Builds/stone-reward-harvest-dtos-6.log at 14:01:57 / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Existing Unity lifecycle exceptions remain.

## 2026-09-11 14:10 — compound members and historical stash verified

Migrated 38 compound-name references in 18 files (compound-members.json): RewardStone, RepairStone, StoneProductionMult, perStone, buyStone. Added explicit existing JSON keys and Unity field aliases to all seven definitions. Extended proof loads original payloads and verifies exact values, old outgoing keys, no duplicate new keys and no retired public members. Historical LootStash.Food is now LegacyFood, retaining its original food key separately from its pre-existing Stone value; proof preserves both 20 and 5 without merging or awarding either. SaveSchema validation and test fixture follow the explicit legacy member. Narrow reflection/property-string search found no references to the retired compound names.

Terminal PASS Builds/stone-compound-members.log, fresh STONE_ECONOMY_INTEGRATION_OK, zero compiler errors. Existing Unity lifecycle reload/shutdown exceptions remain. Refreshed raw inventory: 1,901 occurrences in 299 files, including compatibility keys, comments and tool definitions; not a count of remaining gameplay bugs. One-time compound migration tool now checks its ledger before any mutations.

NEXT POINTER: classify remaining authored/localization/string-key and internal variable references in current.csv, inspect conditional Android/Google Play paths, reconcile reviewed dispositions, then broad release gates. Ordinary settlement durability and other release gameplay requirements remain open. No EXE/APK/AAB artifacts replaced.

## 2026-09-11 14:24 ? broad gate exposed string oracles; visible copy corrected

Builds/release-stone-raid-full.log finished 506/509 green, 3 red, 0 skipped; wrapper FAIL due absent REGRESSION_OK. Two collector source regexes still demanded GrantSpendable(food: amount).Food despite actual production reading GrantSpendable(stone: amount).Stone. Updated both exact assertions, preserving applied-basket and pending-pool protections. KillRewardRaidSuppressionRegression expected EconomyService.Instance; now requires EnsureAvailable() AND eco.Grant(loot). Actual five-axis raid grant behavior is separately tested by RaidLootAuthorityProof.

Corrected the three formerly tolerated player-visible Food strings in guide-content, quests and structures-catalog, in both mirrors. ResourceDevTool private enum, row label and wallet readout now say Stone. RetiredVocabularyRegression has zero copy debt allowances and explicitly detects AddCurrencyRow/SetReadout leaks, with synthetic red controls. No currency values, quest completion triggers or story IDs changed.

Regenerated embedded catalog using CatalogFallbackGenerator: terminal PASS Builds/stone-copy-catalog-regenerate.log at 14:24:04; 29 rows, version42, 110659 bytes, SHA256 6ed22fd7ce0e7e6d0da27d51fd479b1e29b8081baa9931f8e7b5a2fbbc42ace4. Both canonical copies verified byte-identical. StoneReleaseReferenceProof is running next (Builds/stone-release-references.log); do not claim pass until terminal marker. No new full 509/509 proof or artifacts yet.

## 2026-09-11 14:27 ? focused release reference proof passed

First StoneReleaseReferenceProof run failed because the replacement Mill description was 57 characters (limit 48). Shortened it to "Supports town production with wind power."; regenerated catalog via existing generator. Final catalog: 29 rows, version42, 110643 bytes, SHA256 599830614f24b21bf4a07feb5d069917557997ab7c7cea3c7f5840a70c05b167; byte-identical canonical mirrors. Builds/stone-copy-catalog-short.log terminal PASS.

Builds/stone-release-references-retry.log terminal PASS at 14:27:24, fresh STONE_RELEASE_REFERENCES_OK, zero compiler errors. Covers integrated save/economy migration, actual missing-component raid payout and caps, both collector overflow suites, kill suppression/end-payout assertion, and retired vocabulary with ZERO tolerated copy debt. Existing Unity lifecycle exceptions remain. Current raw inventory: 1880 occurrences in 298 files; retained data keys/comments/test fixtures/internal locals still require reviewed dispositions. ConsumableKind.Food refers to provisions, not the retired resource, and needs an explicit inventory rationale rather than blind renaming.

Latest FULL gate remains 506/509 BEFORE the three corrected source-string assertions and text changes; focused retry proves those affected suites, not a new full pass. No Unity batch remains from these checks. No EXE/APK/AAB replaced. Production migration authorization is still pending: only 20260910_0027_play_identity_email_hmac.sql is missing (26 ledger rows); do not run production writes without reply. Original settlement durability, full owned-town build integration, live journey/UI proofs and fresh signed Android artifacts remain release work.
