# Android release plan: raids to owned town to AI arena

Status: PLANNED; execution goal active. Owner request 2026-09-11: APK and AAB focus, raids to arena, highest-logic advisor used frugally. Today is the target, not a guaranteed completion time.

## Outcome and limits

Deliver a signed release-candidate APK and Google Play AAB with the WO-1705 journey: actual final-tier Iron Bastion victory -> one damaged personal town -> repair/design/save/reenter tutorial -> AI practice using the versioned build contract. Preserve the Chapter One castle, existing saves and earned upgrades. AI practice has no wager or ranked payout and must not claim live player matchmaking. The Google Play artifact excludes crypto content. Existing unrelated Arena behavior is not silently redesigned.

Artifact completion, device acceptance and public store publication are separate states. No store publication or production backend migration is implied by this plan. A missing phone does not prevent local work, but device acceptance must stay outstanding.

## Measured starting point

Read on 2026-09-11; rerun these measurements before execution rather than treating them as current forever.

- Branch dev, HEAD 70e8cc8e2; seven commits ahead of local upstream reference; 49 status entries. No Unity process or ADB device observed.
- Newest root canon anchor dated 2026-09-06 names an older HEAD. Newer handovers and source are necessary.
- SaveSchema.CurrentVersion is 41 at source. Builds/test-results-EditMode.xml is absent; do not report an EditMode pass.
- Builds/ready-iter4-regression.log reports 507/507 green. This historical pass does not certify future integration.
- Builds/ready-iter4-raid-saved-polish.log reports a fortified_garrison gate_to_keep incomplete path. The later focused ramp test passes; the full saved-scene proof has not been shown to pass afterward.
- Iron Bastion ground proof explicitly checks local navigation only, with no approach markers. Full entry-to-objective reachability remains unproven.
- WO-1705 is untracked and IN PROGRESS. Initial main-tree search did not locate ownership/capture DTOs; recover worktree/handback work before concluding it must be written.
- APK dated September 10 and AAB dated September 9 predate unfinished work. Two F8 captures remain queued; desktop/device producers were stale.

## Execution order and exit criteria

### 1. Establish the source and recover existing work

Complete remaining applicable boot/catalog reads; inventory only release-related dirty paths, handbacks and worktrees. Identify current writers before mutation. Preserve unrelated work; no blanket staging, stashing, reset or cleanup. Inspect the two pending F8 captures before diagnosing a defect and restore the documented observer when testing resumes. Classify old diagnostics against the actual build and subsequent trace, not their alarming text alone.

Exit: a file ownership list, disposition of existing raid/structure work, exact source baseline, and a concrete WO-1705 implementation inventory. Do not rebuild systems already in a lane.

### 2. Prove the final raid and existing raid repairs

Recheck fortified garrison after the ramp change. Verify saved scene entry -> breach/gate -> objective, spawn placement, victory and return for Iron Bastion. Add genuine endpoints to coverage where necessary; a nearby pair of navmesh points cannot substitute. Use existing generators, never hand-edit scenes. Inspect saved-scene frames for floors, walls, ramps, colliders and visible objectives.

Exit: complete paths to actual endpoints and successful final-tier outcome/return evidence; no unresolved release-blocking saved-scene or render failure.

### 3. Finish one durable captured town

Define acceptance fixtures and save invariants first. Extend existing save/reconstruction contracts with separate ownership, stable structure IDs, condition, layout revision, capture receipt and onboarding milestones. Only actual final-tier victory grants eligibility. Earlier victories, the 20-win entry counter and historical claim flags do not. Carry settled damage into the inherited town, reuse real repair costs, and make essential supplies a one-time recorded grant. Persist ownership, damage, assistance and milestones atomically or with explicit recoverable transaction stages. Retry/load interruption must not duplicate ownership or supplies. Keep story BaseLayout isolated.

Exit: legacy-save load, ownership/layout/damage round-trip, duplicate capture/grant, interrupted claim recovery and story-layout isolation tests pass. Test reload immediately before and after ownership, supplies, repair completion and each tutorial milestone; failed service actions cannot advance progress. Unconvertible structures remain recoverable; no silent deletion.

### 4. Connect tutorial and AI practice

Reuse the tutorial interpreter and placement/repair services. Progress on successful repair, meaningful design change, persisted save/reentry, and completed AI practice outcome, not merely opening or dismissing a panel. Feed the validated saved build revision into the existing arena adapter; AI and future player sources share a contract. Loss has a retry/improvement path and can complete practice. Defer matchmaking, live networking, escrow and ranked certification.

Exit: the player can claim -> interrupt/restart -> repair -> rearrange -> save/reenter -> start practice -> finish win or loss -> return with the same town. Malformed/stale snapshots fail safely, and practice changes no wager/reward balances. New visible copy follows the existing localization contract.

### 5. Freeze and validate the candidate

Run focused seam tests while implementing; only after integration settles run the full compile/regression and relevant UI capture ladder. Open the PNGs at phone landscape dimensions and check touch targets, clipping, missing art and navigation. Verify GOOGLE_PLAY compile paths before the expensive final AAB. Resolve observed failures without weakening assertions. Commit reviewed release changes by explicit path with WO/result/canon evidence; preserve unrelated files through an isolated release checkout if required by the clean-build rule.

Exit: fresh required gate markers cover the exact candidate; no known release blocker remains. Record the gameplay source hash separately from per-build generated version stamps.

### 6. Produce the two artifacts sequentially

Read the full sanctioned scripts and submission checklist before running them. Check Unity/project locks, disk and commit-charge headroom. Use overnight-apk-build.ps1 without -Tester for the store-shaped APK, then google-play-aab-build.ps1. Do not run Unity builds concurrently. Validate each artifact's identity, signing and version, and retain each build's content/catalog proof through tools/r2-ship.ps1. Verify required native/ZIP alignment for APK, physical crypto exclusion and bundletool size for AAB, and restoration of transient Play content/localization changes. Do not use raw AAB zip size as delivered-module size proof. Verify the second build does not invalidate the first artifact's content.

Exit: two fresh signed files, successful platform checks, exact version/catalog linkage, source identity, byte sizes and SHA-256 hashes recorded. No debug/test-only tooling in store candidates. Play crypto exclusion must agree across build configuration, dependencies and packaged-content inspection; a filename or string scan alone is insufficient.

### 7. Device acceptance and handoff

When the Seeker is available, use the sanctioned install path, preserve its save and verify installed version. Inspect an in-place update and the full journey, including restart during onboarding, repair affordability, arena exit and save retention. Exercise the Play variant with its proper device-testing path. Report device-only checks honestly if unavailable. Owner judges gameplay feel; automated gates cannot close that acceptance.

Exit: artifact links and evidence table, device pass or explicit outstanding acceptance, concise release notes and remaining publication steps. Mark the execution goal complete only when its required deliverables and validation are actually satisfied; no success declaration merely because the day ends.

## Usage discipline

- One bounded highest-reasoning advisor review of this plan, with a short context and response; no recursive delegation or repeated advisor loop.
- Default to local implementation and scripts. No broad backlog audit, bulk historical-doc rereading, unrelated Pi work, new art generation or new infrastructure.
- Batch independent reads; constrain searches and output. Keep a short checkpoint of baseline, edits, markers, blockers and next command to avoid rediscovery.
- Run focused checks per change and the full ladder on the settled candidate. Rebuild only after a relevant change or failure; aim for one final APK and one final AAB, without promising that failures cannot require another.
- No paid external service or purchase is requested. No numerical token budget was supplied, so none is invented; frugal execution is a preference, not a guaranteed billing cap.
- If the full journey cannot meet acceptance today, report the exact unfinished seam and retain the last verified artifacts. Do not silently cut the feature, weaken gates or label a partial candidate final.

## Advisor review

Completed 2026-09-11 by one gpt-6-astra advisor at ultra reasoning, without a full conversation fork or additional agents. The advisor endorsed the plan and required exact source identity, interruption-safe ownership/grants, full saved navigation proof after the focused ramp fix, and layered Play crypto exclusion. These are incorporated above. Keep the implementation to one complete progression path; no general ownership framework or broad tutorial rewrite.

Release acceptance stops on unresolved source ownership, failed saved navigation, save loss, duplicate assistance, incorrect ownership, broken tutorial/practice completion, or unexplained crypto content. Missing device access leaves device acceptance pending rather than invalidating honest local artifact results.

## Execution checkpoint

Post-advisor-packet navigation work: RaidNavBake now includes the separate owned town, excludes DefenseTower descendants from NavigationStatic, and installs one runtime carving box enclosing each tower's actual solid colliders. EditMode initializes the same EnsureContactCollider runtime uses before measuring. All five scenes rebaked without regeneration: release-movable-tower-nav.log passed RAID_NAV_BAKE_OK at 11:02:59 (41 towers across 4 raids + owned town), zero compiler errors. The first attempt exposed Unity's fake-null component placeholder bypassing ??; changed to explicit Unity null checks, also corrected the same pattern in OwnedTownController's panel creation. Fresh release-movable-tower-route.log passed FINAL_RAID_ROUTE_OK at 11:03:57, zero compiler errors. Saved entry/staging/boss/objective reachability remains valid; actual runtime carve movement, destroyed breach traversal, and global connectivity after edits remain unproven. OwnedTownDesignService's current immediate commit still needs the advised post-carving route validation/rollback policy.

Advisor share packet is a fixed snapshot at C:/Users/Elden/OneDrive/Desktop/IronBastion_Advisor_Packet_2026-09-11.zip (28 files, 295313 bytes), with README, follow-up questions, reviews, relevant source and SHA256SUMS.csv. It predates the above navigation edits. Latest external response was read and evaluated in docs/RELEASE_ADVISOR_RESPONSE_REVIEW_2026-09-11.md: adopt stable template IDs/layout context, recoverable pending capture, dynamic navigation and explicit no-wallet practice, with corrections to fabricated pristine damage, capture-time-only IDs, split supply stages and abandon-as-completion.

Town controls checkpoint: OwnedTownPanel now builds through the existing kit and calls real reveal/repair/design/reentry services. English keys added to both en.json twins; three contextual tutorial steps added to both tutorial JSON twins; TutorialFlow may arm contextual guidance in owned town without restarting the founding chain. SceneRouter.GoOwnedTown uses the existing pre-load hero carry seam; actual seating/entry remains untested and final victory still routes to Castle. UICaptureLaunch.RunOwnedTownPanelCapture renders reveal/design at three landscape sizes using isolated state. Visual inspection caught Return to castle outside the design frame despite initial automation PASS; combined movement buttons into one row and shortened labels after a measured glyph failure. Final release-owned-town-panel-final.log passed six captures at 10:55:51; final 2670x1200 PNG inspected with all buttons inside the frame. No new APK/AAB; full gameplay integration remains incomplete.

Design/reentry checkpoint: added OwnedBaseProgression.TryChangeInheritedPose. It requires the repair lesson, a standing inherited structure, unchanged template identity/art scale, and a meaningful move/rotation; position+LayoutChoiceCompleted commit as one detached revision and still require reentry. OwnedTownDesignService.TryMove is the gameplay adapter: only defense towers in the owned scene, finite destination inside the 54 m plot, level NavMesh support and physics overlap checks, then durable revision commit before moving the live transform. It does not write story BaseLayout. OwnedTownController.Start now confirms and saves reentry after successful reconstruction when a saved design awaits it. Fresh release-owned-town-design.log passed OWNED_BASE_STATE_OK with zero compiler errors at 10:43:14; tests include no-op/repair-order/resize rejection and atomic detached meaningful move. Physics placement and live reentry still need Play proof; the service is not yet connected to player controls. Relocated towers also need dynamic navigation treatment (old baked tower footprints currently remain). Next is the owned-town UI and tutorial interpreter integration, then routing/hero seating and shared-build AI practice. No release artifact exists for this candidate yet.

Owned-town reconstruction checkpoint: created Assets/Scenes/OwnedTown_IronBastion.unity with OwnedTownSceneBuilder from the saved approved raid (SaveScene to a separate destination; source raid not saved). Removed its single RaidGarrisonSpawner, set 10 turrets PlayerOwned, appended OwnedTownController after template roots to preserve source addresses, registered the new scene in build settings. release-owned-town-scene.log passed OWNED_TOWN_SCENE_OK, zero compiler errors. OwnedTownScenePose explicitly permits the owned-scene/template mapping. BaseLayoutLoader refuses story-layout replay there. WallSegment/DefenseTower/RaidSpire expose owned-scene-only saved-condition restoration, bypassing combat mitigation; ruined structures use existing collapse/destruction behavior. Spire faction is Friendly only in the owned scene.

Fresh release-owned-town-reconstruction.log passed OWNED_TOWN_RECONSTRUCTION_OK at 10:39:25 with zero compiler errors: all 210 walls, 10 towers and 1 spire restored to exactly 0.375 saved health and original position; towers/spire friendly, no garrison, input save unchanged, duplicate reconstruction refused. Proof does not save either scene. This EditMode check covers nonzero condition only: ruin animations/deferred destruction and live hero/HUD/navigation still require Play verification. No transition is routed to this unfinished town UI yet; ReturnHome still goes to Castle. Next implement owned-town controls and the existing tutorial integration, isolate edits from story BaseLayout, route final victory and repeat entry with hero seating, confirm persisted design reentry, and connect the shared snapshot practice adapter. Latest full suite predates capture/town code, and both release artifacts remain pending.

Capture integration checkpoint: RaidCaptureCensus records inherited identities/poses before combat and measures condition once at victory, including destroyed Unity objects as zero-condition ruins. Its settled clone/receipt persists across failed-save retries, and existing owned property is never replaced or replenished. One-time supplies cover the cheapest actually damaged structure's existing repair quote. RaidVictoryController now creates the census at Start, commits only final objective destruction or a cleared nonempty spawned garrison, and retains Return for retry if required capture is unsaved. The controller is explicitly moved into its raid scene to handle additive loads. Return still routes to Castle: owned-town reconstruction/context and tutorial/practice remain to implement. New save-retry toast still needs localization. An entirely undamaged garrison-only win also needs an explicit tutorial path (no fabricated battle damage).

Saved turret identity fix: generator now assigns DefenseTower.CatalogId; StampFinalRaidTowerCatalogIds updated only metadata for all 10 saved final-raid turrets using the existing catalog palettes, with no regeneration. release-final-tower-ids.log passed FINAL_RAID_TOWER_IDS_OK. Focused release-raid-capture-census.log / runner passed RAID_CAPTURE_CENSUS_OK at 10:32:18 with zero compiler errors: census 221, actual wall damage retained, destroyed turret retained, unavailable-save retry frozen, detached clone. The proof also reran geometry round-trip and OwnedBaseStateRegression. Initial EditMode attempt lacked the runtime catalog startup; proof now invokes the actual CatalogBootstrap.Register. This is focused scene/adapter evidence, not an observed full Play victory or durable capture/restart journey. Latest full 508/508 pass predates these capture edits; APK/AAB still unbuilt.

Fresh corrected full regression PASSED: release-regression-fixed.log has anchored REGRESSION_OK 508/508 suites, zero red/skipped/compiler errors; wrapper confirmed fresh PASS at 10:22:04. This precedes the following pose addition. Added optional OwnedStructurePose to OwnedBaseStructure with detached cloning and finite/nonzero-scale/normalized-quaternion/source-address validation. OwnedTownScenePose captures and restores exact local transforms against a loaded scene template, resolving all identities before any mutation. Focused release-owned-town-pose.log emitted OWNED_TOWN_POSE_OK structures=221 plus OWNED_BASE_STATE_OK: actual saved Iron Bastion walls/towers/spire round-trip JSON/local transforms and retain world placement; a missing last identity leaves all earlier transforms untouched. No scene was saved. This is the reconstruction geometry adapter, not yet the live census/victory/damage/tutorial/arena integration. Next: capture identities before combat destroys objects, retain settled condition, commit only actual final victory, then call reconstruction from an isolated owned-town scene context.

Release checks after editor closure: release-compile-current.log passed COMPILE_GATE_OK (zero Assets compiler errors; 21 package WebGL diagnostics classified advisory). The full release-regression-current.log actually FAILED 504/507 despite its wrapper PASS: substring matching accepted COMBAT_FOUNDATION_REGRESSION_OK. The runner now requires complete marker boundaries and no longer calls a missing marker a licensing failure when Unity exited successfully. Judge-only validation rejects that failed suite with exit 8 and accepts the compile log with exit 0. Corrected the three measured suite failures: enabled the now-authored Iron Bastion in build settings; updated the portrait source oracle to require the square FitInParent wrapper; registered OwnedBaseStateRegression and made its live-singleton isolation rejection an exception caught by its verdict method. Brace checks passed 3/3. Fresh full rerun is release-regression-fixed.log (runner process 17580); result pending. No APK/AAB built; remaining gameplay integration and runtime acceptance are unchanged.

Final HUD spacing follow-up: visual review of the passing 1920x1080 capture showed Deploy All too close to its decorative frame. Expanded its command-row span to .410-.715 and Rally to .735-.955, using the space freed by Retreat. Reran the populated capture: fresh release-populated-raid-hud-final.log / runner PASS with zero compiler errors. Reopened the final 1920x1080 PNG and confirmed comfortable frame clearance. Layout is preserved; these are HUD-only changes.

Resumed verification after owner closed Unity: current OwnedBaseStateRegression passed with fresh OWNED_BASE_STATE_OK and zero compiler errors. Navigation rebaked successfully (release-breach-nav.log) without regenerating the accepted layout. Final route proof initially failed: the boss's runtime snapped point is 1.62 m above its authored marker, so the proof now invokes the actual garrison SnapToNav calculation. The remaining objective approach failure exposed a real hitbox bug: RaidSpire assigned world renderer dimensions as local CapsuleCollider dimensions under its scaled root, making its hitbox much smaller than the art. Corrected world-to-local size/centre conversion; FINAL_RAID_ROUTE_OK now passes in release-final-spawn-route.log. This is saved navigation plus actual spawn/hitbox initialization, not Play victory or dynamic-breach proof.

Populated HUD evidence: RunPopulatedRaidHudCapture installs an isolated in-memory GameState with the four observed saved troop IDs and verifies Resources.Load<Sprite> for each. Six frames at three landscape sizes now pass UI_GEOMETRY_OK, UI_TOUCH_OK and UI_GLYPH_OK (36 labels, no baselines or unproved labels), plus POPULATED_RAID_HUD_CAPTURE_OK. The first run found Retreat's height below the 112-reference-pixel minimum; its top-right band is now .850-.970, and the unchanged-height readout moved down to .475-.835 to leave a gap. Inspected the final Seeker readout and 1920x1080 populated tray images. Other remaining acceptance includes the all-deployed state, composite live HUD, dynamic breach, actual victory/return, and captured-town/tutorial/arena integration.

Verification handoff required: the same interactive-editor lock has persisted across three goal continuations. Process 55672 remains responsive; DeNelle.Village.dll is still dated 09:17:27, before the repaired service (09:29:25) and HUD edits (09:28:59). Mandatory Unity compilation, rebaking and runtime verification cannot safely proceed alongside this open test session. Independent source checks are complete; further implementation would stack unverified work on this gate. Goal marked blocked pending owner closing Unity and resuming. On resume, check actual processes first, run focused compilation/owned-base regression and HUD capture, then navigation bake plus runtime breach/final-route proof. Do not regenerate the approved layout. APK/AAB and the remaining captured-town/tutorial/practice integration are incomplete.

Legacy roster check: Builds/troop-alias-check compiles the actual TroopCatalog.cs and TroopDef.cs against current canonical JSON (filesystem adapter for CanonicalJson, throwing Debug stub). TROOP_ALIAS_SOURCE_OK confirms each of the three observed saved IDs resolves to the identical current catalog object, has a portrait file and combat/model fields, introduces no roster rows or alias collisions, and keeps unknown IDs refused. This is not Unity sprite/render validation. Editor 55672 remains live and the gameplay assembly timestamp is still 09:17:27; owner has been asked to close Unity when ready for the pending rebake/gates.

Breach verification tool: added an opt-in, Editor-only RaidBreachRuntimeProof (Defenders/Raids/Observe Next Combat Breach). It observes existing combat without applying damage or saving: records an intact wall's blocked centre and two measured sides, waits for its actual Collapsed event, then allows up to 2 s for carving to update and requires both a walkable centre and an unobstructed navmesh ray across the wall. Emits RAID_BREACH_RUNTIME_OK/FAIL per observed breach. This probe has not been run or Unity-compiled yet; it is intended for the pending post-bake Play test, not a substitute for the final objective route proof.

Navigation follow-up: live logs showed troops reporting NOT-WALKABLE holes after breaking walls. RaidNavBake marked wall renderers NavigationStatic, leaving permanent cutouts, while WallSegment.Collapse only disabled colliders/child obstacles; WallNavObstacleInstaller targets castle WallBarrier names rather than these raid segments. Baker now excludes WallSegment subtrees from static navigation geometry and fits removable carving obstacles to their existing solid box colliders. No scene regeneration or rebake has been run for this change. Required next evidence: after bake, verify intact-wall blocking and reopening after collapse in Play mode, plus real final objective approach. An EditMode path through uncarved ground alone cannot prove runtime integrity. Also corrected OwnedTownRepairService's ResourceCost name collision with an explicit CoreCost alias after observing CS0029/CS1503; brace check passes, Unity recompile remains pending while owner is in Play.

Owner HUD follow-up: requested Retreat move to top right because its bottom label truncates, and requested polish of timer/Spire/Razed/troops. Added shared RaidRetreatBand (x .780-.995, y .885-.970), moved the existing button and handler to that canvas band, centered the timer, increased readout padding, left-aligned brighter status text and reduced the three progress tracks to .018 of panel height. Troop portraits now sit inside a 1:1 AspectRatioFitter wrapper so narrow tray cells cannot stretch the medallions into ovals (observed in the second screenshot). Three-file brace check passed. Actual runtime/layout capture must be repeated after Play restart; no visual pass claimed yet.

Owner feedback: current Iron Bastion layout is accepted ("absolutely perfect"); no manual offsets/handoff are needed and no further layout redesign is requested. New observed UI defect: three deployment portraits were blank. Read-only inspection of saved army IDs found militia x2, archer x2, spearman x1, troop-footman x3; the first three were absent from TroopCatalog.Find while corresponding current portrait assets exist. Added data-driven legacyIds to matching troop rows in both canonical JSON twins and lookup fallback after exact IDs. This preserves saved roster entries and makes combat definition/icon lookup consistent. Owner also requested Deploy All disappear after deployment; RefreshTiles now hides it whenever no deployable troops remain, matching existing spent-tile behavior. Brace checks and JSON byte parity/unique-alias/portrait-file checks pass; runtime visual recheck is pending after Unity reload. Other observed live evidence: PathPartial to raid objective and a destroyed-wall hole reported not walkable; do not treat layout aesthetic approval as navigation/combat acceptance.

Repair transaction: OwnedBaseProgression.TryRepair proposes a detached revision containing supply spend, full repaired condition and EssentialRepairCompleted together. It rejects missing targets, unrevealed ownership, negative/crystal quotes, insufficient supplies, exhausted revisions and already-repaired targets. OwnedTownRepairService resolves CatalogRegistry cost through the existing WallRepairController.CostForFraction and commits through TryCommitOwnedBaseRevision. Scene repair controls/live condition restoration are not wired yet. The root-source standalone harness now passes 30 checks; Unity regression fixtures were extended but not run. Latest new Village source has not been imported/compiled yet (meta absent; do not claim a Unity compile pass). Multiple interactive Unity processes are live; no batch editor launched and no scene edits made.

Owner-layout handoff preparation: RaidLayoutHandoff adds Defenders/Raids/Export Saved Layout Handoff. It refuses unsaved or Play-mode scenes, exports all transforms (including inactive children), prefab/mesh/material references, component type inventory, and a byte-for-byte saved scene/meta backup with SHA-256 under Builds/raid-layout-handoff. It does not modify the scene. Brace check passed; the owner's Unity process 55672 opened D:/EoA, imported the new script, and produced Assembly-CSharp-Editor.dll at 09:19:53 with no error CS lines in Editor.log. Export has not yet been exercised. Scene operations remain with the owner; do not run a batch editor or regenerate over their work. Capture integration still needs a precombat census and preservation of fitted wall transforms: ordinary 3 m grid cells alone cannot faithfully represent these walls.

Save/reentry lesson: added reenteredLayoutRevision without changing existing milestone bit values. Practice completion now requires the saved-layout reentry record; confirmation accepts only the matching reconstructed revision after the layout lesson, is idempotent, and revision acceptance refuses removal/change of a completed record. Updated Unity regression fixtures (not rerun while the scene is offered for owner editing). A standalone harness at Builds/ownedbase-check references the current root DTO/progression sources and passed 23 checks, including mismatch refusal, JSON round-trip and reentry rollback refusal. Brace checks passed three modified sources. This proves the pure contract only; the town reconstruction adapter still needs to call confirmation after actual successful load/reconstruction and durably commit it.

Final-tier follow-up: added BuildFinalRaidScene using the authored iron_bastion config and existing scene path Assets/Scenes/RaidBase_IronBastion.unity. Generation and navigation bake passed in release-final-generate.log / release-final-nav.log. FinalRaidRouteProof initially stopped because EditMode had not initialized RaidSpire's runtime collider; the proof now invokes the actual idempotent EnsureHittable method before measuring the approach. That correction is not rerun yet. Chain process 36184 and Unity both exited. User offered to author the layout; awaiting their choice before another Unity batch or scene regeneration. If they edit the scene, capture their authored transforms before any generator rerun.

Persistence follow-up: GameStateService now exposes TrySave and TryCommitOwnedBaseRevision. The latter saves the accepted detached property (including supplies/milestones) and restores the previous live property on reported failure. Fresh release-owned-base-commit.log emitted OWNED_BASE_STATE_OK; the runner passed with zero compiler errors. Added injected-write-failure coverage checks live rollback, unchanged durable envelope/recency, successful retry, caller isolation, and service Load restoring spent supplies. This uses an in-memory provider and does not certify disk/power-loss behavior. Unity also logged lifecycle reload/shutdown NullReferenceExceptions; resolve or classify before final release gates. The corrected south-gate saved capture was visually inspected and shows a level, unobstructed entrance. Local api/game/save.js buildState preserves incoming camelCase fields; this is source evidence only, not deployed-backend validation.

Latest checkpoint: the second sequential raid recovery run completed with RELEASE_RAID_RECOVERY_OK and a fresh RAID_POLISH_SAVED_PROOF_OK marker. The saved fortified keep route passed; a measured 0.635 m south-gate sill exceeded the 0.4 m navigation step limit, so the generator now seats the gate threshold at ground level before baking. This passes the existing three-scene saved proof, not final Iron Bastion gameplay or Android acceptance. Next: inspect updated gate captures, prove Iron Bastion's actual route, then connect the recovered ownership contracts to victory, repair/tutorial and practice services. APK and AAB remain unbuilt for this candidate; the goal remains active.

2026-09-11: recovered eight Core/save/regression source files and four existing meta files from D:/eoa-ready-board (the four modified tracked files applied via checked patch; the four new sources copied with metas). Brace scanner passed 8/8. Existing standalone pure-contract harness passed 17 checks against recovered sources; it does not prove Unity persistence or gameplay integration. Pin legacy migration fixtures to schema 40 rather than CurrentVersion minus one. Unity persistence validation is next. Local runs set EOA_LOCAL_STATUS_ONLY=1 so nested status-post calls do not send Discord messages; no outbound status authorization is assumed from this release request.

Focused Unity run Builds/release-owned-base-1.log subsequently emitted OWNED_BASE_STATE_OK (capture/retry, milestones, revisions, migration, JSON, snapshot/apply, arena authority). This does not cover the unwritten Village adapters or a real disk restart. Ramp producer mtime is September 10 21:26:30 versus saved fortified scene 21:14:11, confirming the saved scene predates the correction. Sequential recovery chain at Builds/release-raid-recovery.ps1 regenerates the three existing config scenes, rebakes all raid navigation, then checks full saved polish; it stops on a failed stage. Iron Bastion is not in the three-config generator and still requires its own full-route proof.

2026-09-11 11:15 checkpoint: stable GUID IDs baked into both saved Iron Bastion templates (221 objects) without geometry regeneration. release-owned-template-ids.log passed; release-stable-id-capture.log passed exact pose/census/state checks. New pristine inspection requires reveal, refuses actual damage, preserves health/supplies and retries idempotently. Shared first-repair selector drives both capture allowance and panel action; when no tower survives it funds the cheapest damaged tower, otherwise the cheapest damaged structure. release-restoration-edges.log passed at 11:13:38 with zero CS errors, including actual total-destruction census and affordable tower restoration preserving other ruins. Stable-ID town reconstruction proof now running under runner PID 54792, log release-stable-id-reconstruction.log; inspect its terminal evidence before another batch. No C# edits while Unity runs. Latest external response reviewed in docs/ADVISOR_PACKET_RESPONSE_REVIEW_2026-09-11.md and copied to Desktop/IronBastion_Advisor_Response_Review_2026-09-11.md. Original share ZIP intentionally remains an earlier fixed snapshot. Remaining: durable restart settlement, complete layout/economy adapter, live navigation validation, shared snapshot practice importer and mode, town entry/reentry journey, updated captures/gates and final artifacts/device checks. Goal remains incomplete.

2026-09-11 11:19 checkpoint: completed missing first-capture destination connection. RaidVictoryController labels the action Enter your town and routes a required, committed capture to SceneRouter.GoOwnedTown; other raid returns retain castle destination. HeroControlEnsurer now recognizes the owned scene for hero recovery/rehome/seating (previously the carried hero could remain in DDOL). Added optional EndStateVM.PrimaryGate; EndStateView checks it before consuming the primary action, so a failed capture save leaves the button retryable. Capture retry text localized in English JSON twins. release-town-entry-compile.log passed reconstruction with zero CS errors. release-town-entry-retry.log emitted OWNED_TOWN_ENTRY_RETRY_OK (two refusals, no route, primary retained, no latch) plus OWNED_TOWN_RECONSTRUCTION_OK (210 walls, 10 towers, 1 spire; saved conditions and positions); Unity exited zero. Inspect runner terminal verdict after settling. Existing package Lifecycle NullReference diagnostics persist and need classification in final gate audit. These focused EditMode checks do NOT prove live hero transfer, visible button fit, or the complete Play journey. Remaining repeat entry door must respect the approved two-card Journey deck (JourneyDeckTwoCardRegression), so do not casually add a third card. Durable victory recovery, full build adapter and practice integration remain open.

2026-09-11 11:26 checkpoint: added repeat town entry in PlayerDeckWorkspace Realm menu as Enter your town, visible only with a validated OwnedBase. It uses GoOwnedTown independently of raid army/capability gates, preserves the Journey two-card layout and all four Realm services. Initial fifth-card layout failed capture; replaced it with a distinct button in the top body band. Canonical store name still reads HudStrings.StoreFaceLabel("realm-deck"); widened its title plate locally instead of introducing a duplicate short-name string. UICaptureLaunch.RunOwnedTownPanelCapture now captures nine frames (reveal/design/Realm at three landscape sizes) under isolated GameState. release-town-return-ui-canon.log emitted OWNED_TOWN_PANEL_CAPTURE_OK, UI_GEOMETRY_OK, UI_TOUCH_OK and UI_GLYPH_OK (9/9, 66 labels); Unity exited, runner may still be settling. Final standard-aspect capture visually inspected; previous geometry-equivalent ultrawide inspected. No live scene transition or device proof inferred. Prior UI failures were actual text/geometry failures, despite wrapper incorrectly classifying them as license errors. Remaining goal unchanged: durable settlement recovery, full layout adapter, live carving/navigation/hero journey, shared snapshot AI practice, full gates and release artifacts.

2026-09-11 11:34 checkpoint: capture persistence now gates RaidVictoryController before the handled latch, claim, cooldown, loot, army reconciliation and counters. Save refusal shows a retryable EndState screen; its PrimaryGate preserves the button, and abandonment schedules an in-scene retry. This fixes proceeding with rewards after a failed capture save, but is not a transaction across all raid effects. Added typed, optional PendingTownCapture in local signed SaveSchema/GameState snapshots. RaidCaptureCensus records the frozen capture before consuming it. GameStateService.Load invokes TryRecoverPendingTownCapture without any old scene/census object: ownership + isolated supplies + intent removal share a write; failure restores live state and retains the durable intent. Repeated load preserves spent supplies. Local Load treats pending-record absence authoritatively. Backend application defers while local intent is unresolved, cannot install remote intent, and newer reset epochs clear local pending capture. No universal schema bump imposed for optional/default-on-read field. Pending intent is local-only and not included in BuildSaveBody. release-pending-capture-recovery.log passed at 11:32:27 zero CS: actual Load recovery, injected failure retention, immutable census, conflicting receipts refused, no supply replenishment, pose/census checks. release-capture-recovery-cloud.log emitted OWNED_BASE_STATE_OK for added cloud boundaries; runner may be settling. No real process-kill/disk durability test or complete win-to-town Play test yet. Ordinary loot/counters/quests/cooldown/companion/army effects still span different stores and are not recovered by PendingTownCapture. Do not claim full victory crash safety. Full layout adapter, live carving checks, practice importer/mode, whole-journey gates and APK/AAB remain open. No live Unity process observed at checkpoint.

2026-09-11 11:45 checkpoint: added shipped structural manifest Assets/Resources/OwnedTown/IronBastionTemplate.json (221 actual saved structures, stable template IDs, catalog/level, original fitted pose and collider bounds), produced by OwnedTownManifestBake.Run without scene regeneration/save. OwnedTownLayoutSnapshot implements the production structural authority for detached ArenaBuildSnapshot (new templateVersion field); validates template/ruleset, hero enum, exact census, unique IDs/legacy path+name fallback, catalog/level/shape/frame, immovable perimeter, moved-tower plot/elevation and conservative overlap bounds. Combat roster/loadout payloads are explicitly not approved by this structural-only ruleset; full practice match authority/import remains open. OwnedTownController reconstructs from its validated detached layout, and OwnedTownDesignService validates proposed layout before committing. No complete BuildMode adapter yet.
Overlap refusal test found the old DefenseTower generated capsule copied world dimensions into local fields on 0.01-scale tower roots: collider was ~100x too small. EnsureContactCollider now converts world height/radius/center into local space and repairs only the matching legacy generated signature, preserving authored solid colliders. Bound shrink is proportional, avoiding negative extents on small footprints. release-tower-collider-nav.log passed 5-scene nav rebake at 11:43:00. release-town-layout-contact-proof.log passed at 11:44:05: real contact point on scaled legacy fixture, idempotence, unchanged art/authored collider, 221 manifest round-trip, legitimate tower move accepted, overlap/unknown IDs/catalog/out-of-plot/scale/version refused. Earlier authority test failed overlapping towers and drove this fix (not a license issue despite wrapper wording). release-layout-integration.log passed at 11:45:11 zero CS with FINAL_RAID_ROUTE_OK + OWNED_TOWN_RECONSTRUCTION_OK + OWNED_LAYOUT_INTEGRATION_OK. This remains EditMode/structural evidence, not live carving traversal or practice combat. No Unity process live at checkpoint. Remaining: full reward/counter settlement recovery, BuildMode operation/economy adapter, live scene/hero/nav journey, shared-build AI practice match integration, full gates and APK/AAB/device validation.

2026-09-11 11:58 checkpoint: replaced synchronous OwnedTownDesignService.TryMove with TryBeginMove. OwnedTownMoveOperation owns a reversible live preview, waits for two NavMesh.onPreUpdate cycles and another frame, validates active-agent-filter entry/staging/courtyard routes, rejects concurrent property changes, then commits. Failed routes/saves, disable/cancellation and scene interruption restore the original transform; prior saved revision is never recommitted as rollback. Panel hides move/exit actions while pending and reports checking/saved/refusal. Navigation reads actual scene agentTypeID/areaMask pairs; BossSpawn allows the canonical spawner's 8m vertical seat search with <=1.5m horizontal displacement because the authored marker lies below a raised courtyard. This is bounded route evidence, not proof of every attack approach.
Added OwnedTownMovePlayProof with SubsystemRegistration in-memory ISaveProvider installed before GameState startup, signed-main-save before/after comparison, scratch runtime NavMesh/carving fixture and real owned-scene load. Hardened tools/run-unity-playmode.ps1 to reject timeout/compiler errors and require whole-token success markers. release-town-move-play.log passed real carve movement before commit, blocked-route rollback, injected write failure rollback, cancellation and unchanged main save. Extended actual-town run initially failed on BossSpawn's y=0 marker vs walkable y=1.62; corrected seating. release-real-town-paths-play.log subsequently passed OWNED_TOWN_REAL_PLAY_OK + OWNED_TOWN_MOVE_PLAY_OK, terminal wrapper PASS at 11:57:52, no compiler errors/timeouts. Actual 221-record town restored mixed .375 health and zero-condition wall/tower/spire, preserved its damage ledger, did not revive ruins or leave their obstacles active, saved reentry and passed live courtyard paths. Hero was the production emergency fixture fallback on direct load, NOT the actual carried raid hero; the true victory->town carry still requires proof. Lifecycle NullReferences in this log are Unity.Scripting.LifecycleManagement.TopologicalSortRecursive, not new gameplay exceptions. Main player save compared unchanged. No Unity process live at checkpoint. Full ordinary reward recovery, complete BuildMode adapter, shared-build practice combat and final artifact/device validation remain unfinished.

2026-09-11 shared-import checkpoint: release-raid-town-hero-play.log reached terminal wrapper PASS with OWNED_TOWN_HERO_CARRY_OK and OWNED_TOWN_MOVE_PLAY_OK. Actual castle hero crossed the final raid scene, entered the owned town and revisited with the same instance, no duplicate or DDOL leak; main signed save unchanged. This invokes scene routing, not victory settlement. Isolated Play harness now suppresses cloud synchronization under UNITY_EDITOR while its in-memory provider is active. Unity lifecycle shutdown NullReferences remain distinct unresolved editor diagnostics.
Extracted OwnedTownSnapshotImporter from OwnedTownController. It consumes a detached, revalidated ArenaBuildSnapshot, preflights every target and requires exactly one condition handler before changing poses/faction/damage; it never reads GameState or writes progression. Controller now uses this importer. Current importer supports the isolated owned template scene and friendly structural restoration only; dedicated practice scene, combat payload authority, AI spawning, outcomes and economic isolation remain required. Added missing-spire failure proof to ensure an incomplete template cannot partially damage earlier walls or mutate input snapshot. Three-file brace check passed. Focused Unity run release-shared-town-import.log started; inspect terminal verdict before relying on it. Do not build final artifacts yet.
2026-09-11 12:09 verification: release-shared-town-import.log terminal VERDICT=PASS, OWNED_TOWN_RECONSTRUCTION_OK, zero compiler errors. Shared importer restored all 210 walls, 10 towers and one spire with exact saved health/pose and preserved source data; late missing-condition-component refusal left earlier structures untouched. Unity lifecycle reload/shutdown NullReferences still present. No Unity process remains. Full practice combat, complete settlement recovery, BuildMode adapter and candidate APK/AAB remain open.

2026-09-11 12:17 owner test handoff: owner requested a Windows EXE with dev panel. build-windows.ps1 completed successfully; Builds/build.log reports 30 scenes DEVELOPMENT, ADDRESSABLES_CONTENT_OK 738 Windows locations, DesktopBuild SUCCEEDED 2084 MB in 1m28s player build. Fresh Builds/Windows/DefendersOfTheRealm.exe plus UnityPlayer.dll/Data/MonoBleedingEdge verified; DeNelle.DevTools.dll present. Desktop shortcut Echoes of Elarion - Dev Test.lnk points there; TEST-NOTES.txt included. Correct entry is Settings > DevTools; F10 is explicitly retired in DevPanelController (earlier shortcut commentary corrected). EXE has not been launched by the agent; owner gameplay acceptance remains outstanding. No new APK/AAB or store publication.
PracticeCombatPolicy now uses actor scene ArenaPractice_IronBastion to suppress Enemy kill XP/resources, ProgressionManager shared XP, and ItemDropWatcher callbacks; death event remains active. Policy captured before Enemy.Died callbacks. release-practice-kill-play.log terminal PASS with PRACTICE_KILL_ISOLATION_OK: real hero XP receiver, actual lethal Enemy.TakeDamage, death callback plus item watcher, unchanged XP and full save snapshot; active scene deliberately stayed town to prove actor-scene policy. No complete practice scene/match exists yet. This is required economic isolation work, not a claim that every practice side effect has been audited. Full settlement recovery and full BuildMode adapter remain open. Unity finished; no batch running at handoff.

2026-09-11 12:23 practice session checkpoint: added Core.State.OwnedTownPracticeSession for a detached ArenaBuildSnapshot bound to the exact saved property revision/content. Entry requires reveal+repair+design+saved reentry and production-supplied authority. Match consumers receive clones. Win/loss result is immutable; only those outcomes can save PracticeCompleted, with failure retaining the resolved result for retry, successful repeat idempotent, and concurrent town changes refused. Abandon/unfinished sessions cannot complete the lesson. This session is not yet wired to a live practice controller or UI; no practice scene, AI match or device acceptance claimed.
Extended isolated OwnedBaseStateRegression persistence fixture to cover wrong snapshot damage, absent reentry, clone isolation, win/loss/abandon, injected save refusal/retry, idempotency and concurrent edit refusal. Initial run failed because the new test fixture incorrectly used the same revision for restored layout and reentry confirmation; fixed fixture to revision2/reentered1, retaining production guard. release-practice-session-retry.log terminal PASS, OWNED_BASE_STATE_OK, zero compiler errors at12:23:22. Windows EXE remains the12:17 owner test artifact and has not been rebuilt or modified. Full objective remains open: practice match/import/AI/outcome integration, full ordinary raid settlement recovery, full build adapter, full release gates and fresh APK/AAB.

2026-09-11 live practice integration: dedicated ArenaPractice_IronBastion scene copied from the approved owned template through OwnedTownPracticeSceneBuilder (no original scene save or geometry redesign), registered in Build Settings. OwnedTownPanel exposes Practice defending town after saved reentry; reenter button occupies that slot only until first reentry. SceneRouter.GoTownPractice carries the same hero; HeroControlEnsurer recognizes/re-homes it; BaseLayoutLoader skips story layout there. Snapshot importer/pose resolution and wall/tower/spire condition guards now explicitly allow only town/practice copies; spire faction is friendly in practice.
OwnedTownPracticeController freezes OwnedTownPracticeSession on entry, imports damaged structures, validates live routes, spawns three hollow-walker attackers via GarrisonStatBlocks/EnemyFactory/EnemyBrain at entrance slots, and resolves from actual Died events (unexpected missing enemies cannot count as victories). BattleLock probe guards gameplay panels. Win/loss save the lesson with retry; abandonment skips completion. Hero practice defeat floors HP at1 without global death events/evacuation and restores entry HP on return. ConsumableUseService refuses practice inventory spending with localized feedback. This is local fixed-roster practice on the shared structural snapshot; remote-player combat payloads remain unsupported by this ruleset.
First live attempt failed on the previously town-only WallSegment restoration guard; source guards were extended to the isolated practice scene. release-practice-match-retry-play.log passed win/save-refusal/retry/return. Extended release-practice-outcomes-play.log terminal PASS (OWNED_TOWN_PRACTICE_PLAY_OK): actual AI advancement or exchanged damage over60frames; three-actor spawn; lethal win; failed completion write/retry; actual hero defeat without normal death; abandon; repeated return with cleared defeat flag; saved town structure ledger unchanged. Isolated fixture now sets Onboarded=true to represent a player who reached final-raid ownership rather than triggering initial castle dialogue. No claim of fully audited passive progression, arbitrary loadout transport, attack approach coverage or full release acceptance. Current UI capture release-practice-ui.log is running 18frames (town reveal/design/ready/Realm plus practice active/result at three landscape sizes). Windows12:17 owner EXE is unchanged and does not contain this practice integration.
2026-09-11 12:40 practice UI verification: release-practice-ui.log failed actual geometry/touch/text oracles (not licensing despite wrapper classification): compact header was culled and CTA band fell below112 reference pixels. Adjusted practice panel bounds, fitted header+shadow independently, separated body text and enlarged CTA band. release-practice-ui-fit.log terminal PASS, OWNED_TOWN_PANEL_CAPTURE_OK18frames, UI_GEOMETRY_OK18, UI_TOUCH_OK18/18, UI_GLYPH_OK18/18 with105labels, zero compiler errors. Visually inspected TownPracticeActive_1920x1080, TownPracticeResult_2670x1200 and OwnedTownReady_1920x1080: title and actions readable, no truncated labels. These are isolated UI canvas renders, not full gameplay composition/device acceptance. Current practice outcome Play evidence remains release-practice-outcomes-play.log; subsequent code change was panel geometry only. No Unity batch remains. Owner Windows EXE remains12:17 and excludes new practice code. Full ordinary victory recovery, complete BuildMode operation/economy adapter, wider gates, gameplay composite captures, artifact/channel/remote content checks and fresh APK/AAB remain outstanding.

## 2026-09-11 13:04 — owner-directed Food retirement, save-first migration

Owner explicitly requested a work order and per-occurrence inventory, starting with save logic. Work order: WorkOrders/WORK_ORDER_FOOD_TO_STONE_2026-09-11.md. Frozen discovery baseline: docs/food-migration/baseline.csv (2,811 occurrences / 340 files). Execution pointer: docs/food-migration/SAVE_FIRST.md.

ResourceBalance now stores Stone with the shipped food JSON key and a temporary nonserialized Food caller bridge. AddFood renamed to AddStone across all five production callers; no method alias remains. Local/cloud alias assignments, validation, time-derived reconciliation, change detection and offline sync DTO use Stone. SyncDeltaPayload preserves the deployed Food key explicitly. BuildJobData.PaidStone and JobCost.Stone replace old runtime fields; cancellation credits wallet.Stone. Original basket wire keys retained, with Unity serialized aliases.

Focused proof terminal PASS: Builds/stone-save-baskets.log / STONE_SAVE_MIGRATION_OK / zero compiler errors. Covers old wallet JSON, repeated round trip, actual Unity wallet asset migration, offline queue key compatibility, legacy paid basket amounts and zero unrecorded refunds, plus core save contract. Initial JsonUtility legacy-field fixture failed; real Unity asset proof passed. Existing lifecycle reload/shutdown exceptions remain. Full queue and release regression reruns are outstanding; Windows tester and Android artifacts were not rebuilt.

Current broad regression evidence remains Builds/release-post-practice-regression.log: 505/508 suites green, six failures concerning HubScenes classification of owned/practice scenes, 29 missing ownedTown keys in nine locales and English Unity table, smart arguments affected by those missing keys, and unregistered ownedTown tutorial signal IDs. These failures are not resolved by the Stone save proofs. Ordinary victory settlement recovery, full owned BuildMode operations, gameplay/navigation proofs and fresh APK/AAB remain required under the original release goal.

## 2026-09-11 13:09 — Stone catalog costs and integrated economy proof

Core.Catalog.ResourceCost now uses stone (old food JSON key and Unity field alias retained). All discovered field callers migrated across 26 files: capture repair supplies, quote validation/spending, BuildMode cost scaling/refunds, wall repairs, UI cost cards and regression expectations. No temporary alias on this cost type. Separate tuple members and old wire strings were excluded. Focused old-cost deserialization passed, then StoneSaveMigrationProof.RunIntegrated terminal PASS in Builds/stone-economy-integration-retry.log at 13:08:58 / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Runs CoreSaveContract, OwnedBaseStateRegression, ObsidianQueueRegression and BuildEconomyRegression. New owned repair assertions verify insufficient Stone refusal, exact deduction and unchanged source revision. Initial combined entry had a missing namespace qualifier; corrected and rerun. Existing Unity lifecycle exceptions remain.

Remaining Stone pointer: legacy dungeon LootStash two historical axes (currently DTO/validation only; do not sum), remaining wallet Food source bridge/callers, other economy/resource enums/types and authored content, reviewed occurrence ledger and backend protocol migration contract. Release-wide six regression failures remain unaddressed; no new APK/AAB or Windows build.

## 2026-09-11 13:17 — scene/tutorial failures fixed; owned-town localization imported

Added explicit OwnedTown and TownPractice SceneKind values (appended to preserve previous enum values), exact scene predicates, calm town HUD handling and active combat declaration for practice. IsHub/Names remain unchanged to avoid enabling unrelated hub bootstraps. All enabled build scenes pass production context/posture resolution: owned town calm(town), practice hostile(activebattle). Canonical OwnedTownRevealed/Repaired/Designed/Reentered/Practiced signal constants now drive production raises and the tutorial registry check. Focused gate terminal PASS Builds/owned-town-release-contract.log / OWNED_TOWN_RELEASE_CONTRACT_OK / zero compiler errors, 22 tutorial steps validated.

Added 29 ownedTown keys to all nine non-English policy locales, both Resources and StreamingAssets mirrors; translations remain AI drafts consistent with existing locale policy. Source dataset and bounded application tool under tools/localization/owned-town-translations.json and apply-owned-town-translations.py. Imported enabled Unity string tables through existing LocalizationBuilder. Builds/owned-town-localization-retry.log terminal PASS / LOCALIZATION_REGRESSION_OK 9/9 suites / zero compiler errors. Parity covers ten policy locales; glyph coverage covers six build-enabled locales (en/es/pt-BR/de/fr/ru). ar/ja/ko/zh-Hans remain internal-limited, not newly enabled. No claim of native-speaker or localized-layout acceptance. Initial importer reference crossed editor assemblies; corrected to explicit reflected importer invocation, then reran.

Full DataRegression launched at 13:16 after the localization terminal PASS: Builds/release-stone-town-full.log, wrapper PID 43128, Unity PID 25036 (confirmed live). Expected REGRESSION_OK, await exact fresh marker and terminal wrapper verdict before edits or claiming the prior six failures are cleared globally. No Android or Windows artifacts rebuilt. Original release objective and outstanding settlement, owned BuildMode, gameplay/device proofs remain active.

## 2026-09-11 — compiler-guided wallet alias removal, in verification

Full release-stone-town-full.log ended terminal FAIL: 507/508 green, zero skipped, one ResourceAuthorityRegression failure with three old-name assumptions. The six prior scene/tutorial/localization failures no longer appear. ResourceAuthorityRegression now checks the actual serialized sync DTO: one legacy Food key with the real Stone value and no second Stone/stone key. It still rejects the retired top-level GameState.Stone authority and checks client/server reconciliation key equality. Updated inbound alias checks to the migrated Stone members. EconomyService and 34 other files now read the real Resources.Stone directly. Focused integrated authority check passed stone-wallet-authority.log with zero compiler errors.

Removed ResourceBalance.Food temporary property and renamed its constructor parameter to stone. Compiler diagnostics then identified 44 exact tokens across four dependency-layer passes (7 + 32 + 4 + 1). Migrated only validated file/line/column Food tokens, including production harvesting/buildings/outposts/mining, dev grants and test/capture fixtures. Reusable tool tools/migrate-wallet-compiler-references.py refuses stale pointers or any error not specifically about ResourceBalance.Food. Append-only evidence ledger docs/food-migration/compiler-wallet-migrations.json records source logs and each pointer. Last gate running: Builds/stone-wallet-runtime-migrated.log, expected STONE_ECONOMY_INTEGRATION_OK; await terminal verdict. Save wire food key and FormerlySerializedAs("Food") remain explicit compatibility boundaries. Other ResourceCost/enums/economy API Food names are still separate migration work. Full release and Android builds remain outstanding.
Wallet alias removal terminal PASS at 13:29:25: Builds/stone-wallet-runtime-migrated.log / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. No ResourceBalance.Food runtime member remains. Full release objective remains unfinished.

## 2026-09-11 — economy API and raid reward type migrated to Stone

EconomyService.Stone, IEconomy.Stone, Village.ResourceCost.Stone/StoneOnly and ResourceSnapshot.Stone replace the Food members; economy grant method named parameters now use stone. No source aliases added. Village reward-cost Food wire field retained via explicit JsonProperty("Food") and Unity FormerlySerializedAs("Food"); readonly snapshot retains its old JSON name. Other resource enums (BankResource/ResourceType/MineResource) deliberately not renamed in this slice; they require separate numeric/string compatibility review.

Compiler-guided caller ledger docs/food-migration/compiler-economy-migrations.json records 243 exact member/argument references migrated across dependency-layer passes. Helper tools/migrate-economy-compiler-references.py checks every diagnostic token position before editing and handles conditional-access dot pointers. One pointer check refused before any file mutation; an unchanged compile was inadvertently launched, then the helper was corrected and migration resumed. IEconomy fake implementations and the bounded CaptureLedger class were migrated explicitly; test amounts and semantics preserved.

Integrated proof terminal PASS Builds/stone-economy-api-11.log at 13:40:49 / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Added legacy raid reward-basket load/serialization assertions. Expanded RunIntegrated with EconomySweepRegression, HonestFeedbackGrantRegression and RaidScoringRegression because payout APIs changed. Updated one source-contract assertion from applied.Food to applied.Stone, preserving its applied-versus-requested requirement. Latest expanded gate Builds/stone-economy-payouts.log has emitted success marker; await terminal wrapper verdict before accepting. Full release rerun and artifacts remain outstanding; Windows tester is unchanged.

Economy payout expansion terminal PASS at 13:42:20: Builds/stone-economy-payouts.log / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Verified suite set now includes save compatibility/core save, owned town, queue refunds, build economy, resource authority, economy sweep, honest feedback grants and raid scoring. Existing Unity lifecycle exceptions remain. Next migration targets are the remaining enum identities and separate resource/harvest/UI DTOs, followed by reviewed residual inventory and channel compilation. Original release requirements remain open.

## 2026-09-11 13:52 — resource enum migration verified

Renamed runtime enum members to Stone with explicit original numeric values: BankResource=2, ResourceType=2, MineResource=2, HarvestResource=1, HarvestTarget=2, ElarionUiKit.CurrencyKind=4. Each retains EnumMember("Food") so string-enum saves stay readable/writable under the deployed legacy spelling without a Food runtime alias. Qualified references migrated in 25 bank and 41 further enum files; compiler also located four aliased BankResource references in Wallet/PackStore. StorageStackView now uses TownBankCapacity.TryParseResource and the new HarvestResourceNames.TryParse boundary so legacy food/grain and current stone names resolve correctly. Existing Echo assignment storage tokens remain unchanged.

Proof now checks enum numeric identities, absence of Food runtime names, old string-enum round trips and unchanged default numeric serialization; checks legacy/current parser names. Added EchoSpecializationRegression and OfflineHarvestRegression to the integrated suite. Initial proof had a HarvestTarget namespace typo, corrected. BuildEconomyRegression then caught a generated catalog hash mismatch because a qualified enum-name sweep also matched embedded catalog text. Corrected by running the existing CatalogFallbackGenerator.Generate from canonical source, not by editing the checksum. Generator terminal PASS: Builds/stone-enum-catalog-regenerate.log, canonical copies byte-identical, 29 rows/version42/110625 bytes, SHA256 b9e2ae5c7ebdefd98b20486696b8e14431d2d3aa49c49a8d74ce8f2d46b965ab.

Expanded integrated suite terminal PASS: Builds/stone-enums-harvest-validated.log at 13:51:43 / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Includes save, town repair, queue refunds, build economy/catalog parity, authority, economy sweep, feedback grants, raid scoring, Echo specialization and offline harvesting. Existing Unity lifecycle exceptions remain. Other harvest/reward/UI DTO Food members and authored/localized references remain migration work. No new full-release pass or APK/AAB/EXE build claimed.

## 2026-09-11 14:02 — reward, harvest and UI DTOs migrated

Renamed Food members to Stone in fourteen definition files: JewelerRecipeCost, GearRecipeCost, PackEconomy, RewardEconomy, RealmClearReward, DailyQuestVM.RewardInfo, EconomyModel, StakesLedger, WaveClearPayout, OfflineHarvestResult, BuildMenuVM, BuildingUpgradeVM, PartyShopVM and TroopTrainingVM. Existing explicit JSON keys (food/stone/f) retained; formerly unannotated result fields retain capital Food JSON keys via explicit attributes. Mutable serialized fields also carry FormerlySerializedAs("Food"). Pack/season legacy-food decoding remains a compatibility boundary, not another balance. Definition-file private lower-case parameter names remain a later naming cleanup, not a source bridge.

Compiler ledger records 57 further exact caller references over dependency layers (1+5+35+15+1), across grants, crafting, HUD/readouts, offline claim/welcome-back, saved siege loss, tests and UI captures. Legacy field proof covers original JSON key/value loading and round-trip for seven representative DTOs, including read-only wave payout. Expanded integrated suite with PackGrantRegression and SiegeLossStakesRegression. Terminal PASS Builds/stone-reward-harvest-dtos-6.log at 14:01:57 / STONE_ECONOMY_INTEGRATION_OK / zero compiler errors. Existing Unity lifecycle exceptions remain.

NEXT POINTER: remaining public member declarations are RepairFood (rf wire key), RewardFood (rewardFood), FoodProductionMult (foodProductionMult), perFood (unannotated authored repair rate), buyFood (AccessoryDef and two GearCatalog types; loader uses JsonConvert), and historical LootStash.Food alongside its separate Stone field. Rename compound identifiers without changing quoted wire keys or generated files; add explicit old-key/Unity aliases where missing. Review reflection and source-contract string references deliberately. Historical LootStash has no identified active reward consumer and must not be merged into current Stone or silently dropped. Residual authored/localization/string-key inventory, broad gates, gameplay integration and fresh release APK/AAB remain outstanding. No artifacts replaced.

## 2026-09-11 14:15 ? reward fallback and build-chain review

Stone compound-member migration terminal PASS: Builds/stone-compound-members.log, STONE_ECONOMY_INTEGRATION_OK, zero compiler errors. RaidLootAuthorityProof terminal PASS: Builds/raid-loot-authority.log, actual GrantLoot restores missing economy component and credits all five axes with ordinary caps and truthful result basket. Cross-store settlement durability remains OPEN. Full DataRegression now running under wrapper PID 23832, log Builds/release-stone-raid-full.log (verify terminal state before any next Unity launch/source edits).

Read overnight-apk-build.ps1, google-play-aab-build.ps1 and tools/r2-ship.ps1 fully. APK/AAB chain builds Android content and invokes parent-wide R2 push plus all-target verification; AAB includes signing input/log checks and bundletool size ceiling 500,000,000 bytes. Final artifacts still require direct package/signature/content inspection, not just script messages. No scripts executed for artifact generation and no EXE/APK/AAB replaced.

Residual Food copy audit identified three explicitly tolerated player-facing strings: guide-content.json sections[2].tips[0] resource list, quests.json quests[6].stages[0].objectiveText, structures-catalog.json entries[13].description (Mill). RetiredVocabularyRegression currently allows these as known copy debt. User authorization permits correcting them; update both canonical mirrors and regenerate catalog fallback, then remove exact debt allowances and verify. Preserve story IDs, reward values and deployed data keys until their explicit caller migration.

### Live schema preflight ? pending owner authorization

`node tools/schema-parity.mjs` failed: TABLE MISSING play_identities. Read-only schema_migrations query found 26 recorded migrations and exactly one pending: api/migrations/20260910_0027_play_identity_email_hmac.sql. Reviewed SQL creates the play-* identity lookup table, optional email HMAC column and partial lookup index; no data deletion or reward mutation. tools/run-migrations.mjs explicitly reserves production execution for the owner. Asked owner for authorization to apply this exact migration; no production write performed. Existing runner must be used after approval, with MIGRATIONS_OK plus fresh CREATE/ALTER parity proofs. Do not mistake elapsed time for approval. This blocks the sanctioned APK preflight independently of local Unity gates.

## 2026-09-11 14:27 ? focused release reference proof passed

First StoneReleaseReferenceProof run failed because the replacement Mill description was 57 characters (limit 48). Shortened it to "Supports town production with wind power."; regenerated catalog via existing generator. Final catalog: 29 rows, version42, 110643 bytes, SHA256 599830614f24b21bf4a07feb5d069917557997ab7c7cea3c7f5840a70c05b167; byte-identical canonical mirrors. Builds/stone-copy-catalog-short.log terminal PASS.

Builds/stone-release-references-retry.log terminal PASS at 14:27:24, fresh STONE_RELEASE_REFERENCES_OK, zero compiler errors. Covers integrated save/economy migration, actual missing-component raid payout and caps, both collector overflow suites, kill suppression/end-payout assertion, and retired vocabulary with ZERO tolerated copy debt. Existing Unity lifecycle exceptions remain. Current raw inventory: 1880 occurrences in 298 files; retained data keys/comments/test fixtures/internal locals still require reviewed dispositions. ConsumableKind.Food refers to provisions, not the retired resource, and needs an explicit inventory rationale rather than blind renaming.

Latest FULL gate remains 506/509 BEFORE the three corrected source-string assertions and text changes; focused retry proves those affected suites, not a new full pass. No Unity batch remains from these checks. No EXE/APK/AAB replaced. Production migration authorization is still pending: only 20260910_0027_play_identity_email_hmac.sql is missing (26 ledger rows); do not run production writes without reply. Original settlement durability, full owned-town build integration, live journey/UI proofs and fresh signed Android artifacts remain release work.

## 2026-09-11 ? owner-approved Play identity migration applied

Owner explicitly authorized 20260910_0027_play_identity_email_hmac.sql. Immediately before execution, read-only ledger check reconfirmed it was the only pending migration. Existing tools/run-migrations.mjs applied exactly one and skipped26, recording all27. Builds/play-identity-migration-approved.log contains MIGRATIONS_OK applied=1 skipped=26 and ALTER_COLUMN_SWEEP_OK (32 ALTER-added columns /28 source files). Subsequent node tools/schema-parity.mjs passed SCHEMA_PARITY_OK48 tables. No store publishing or unrelated production migration performed. Earlier pending-approval checkpoint is superseded.

Owner requested a current SKR-use research list while town integration was being inspected. Answered with docs/SKR_CURRENT_USE_RESEARCH_BRIEF_2026-09-11.md. Release work resumed below.

## 2026-09-11 14:42 ? continued owned-town repairs implemented

Found an end-state gap: OwnedTownRepairService previously spent only the isolated introductory supplies; after the first repair the panel offered no further repairs. Added a supply-first pure repair plan and GameStateService.TryCommitOwnedBaseRepair, which saves condition, allowance, milestone and normal Wood/Iron/Stone debit together. Failure rolls back all four domains; notifications occur only after successful durability. No crystals/coins spent. Existing quote authority remains WallRepairController.CostForFraction. Gameplay adapter validates the repaired snapshot before charging, preventing a restored ruin overlapping a moved tower.

OwnedTownPanel now offers Repair more, cycles damaged structures, shows condition and separate supply/wallet charges, and uses the existing RepairHighlight visual for selected repairs and design towers. The initial funded repair remains the capture service's chosen repair so a zero-resource player cannot accidentally consume the lesson grant on the wrong structure. Subsequent repairs allow selection and normal material spending. Added five keys across both mirrors of all ten locales; updated the translation source/tool to34 keys. Expanded UI capture states from6 to8 (24 renders across three landscape sizes), including initial repair and later paid repair.

OwnedTownRepairPaymentProof terminal PASS Builds/owned-town-repair-payment.log at14:42:07, zero compiler errors: exact mixed supply/wallet debit, failure rollback, unchanged durable envelope/recency, no failed notifications, retry/no double debit, reload, untouched story layout/crystals/coins, insufficient-funds and premium-spend refusal, continued repair with zero supplies. Existing owned-base suite also passes after isolated fixture cleanup. Registered in full DataRegression; full gate not rerun yet. Localization import/regression currently running at Builds/owned-town-repair-localization.log, wrapperPID10424; verify terminal state before next Unity command. UI captures and real scene action proof still pending. This does not claim complete new-building/upgrade/sell integration or ordinary raid settlement durability.

## 2026-09-11 14:56 - repair capture semantics and layout corrected

Earlier owned-town-repair-ui.log PASS was insufficient: visual inspection found missing repair controls because executeMethod never initialized CatalogRegistry. UICaptureLaunch now hydrates the existing canonical catalog, checks repair/next/back actions plus selection/payment presence, and restores the registry after isolated capture. Initial-repair fixture uses the full actual quote; later repair retains a mixed supply/wallet quote. No player save loaded.

Populated rendering then exposed genuine UI defects: castle button overlapping repair costs, selected structure touching the title, and the design exit extending below the frame. OwnedTownPanel now separates selection/hint/payment/commands; later repair exits through Back to design; design groups Repair more and Return to castle on one row with readable label sizing. Intermediate failures retained in owned-town-repair-ui-complete/verified/grouped logs; the first was a missing LINQ extension compile issue corrected with Array.Exists, not a licensing failure despite the wrapper label.

Final evidence: Builds/owned-town-repair-ui-readable.log and runner log: OWNED_TOWN_PANEL_CAPTURE_OK frames=24, UI_GEOMETRY_OK24, UI_TOUCH_OK24/24, UI_GLYPH_OK24/24 (138 labels), zero compiler errors. Visually inspected initial repair at1920x1080, later repair at2670x1200, and final design at2340x1080. Initial repair shows supplies180 Iron and player charge0; later repair shows supply1 Iron/player charge179 Iron. Localization had already passed LOCALIZATION_REGRESSION_OK9/9 in owned-town-repair-localization.log. These are isolated English UI captures, not device or live repair-action acceptance.

Next: extend OwnedTownMovePlayProof with actual Repair more/Repair button invocation, failed save rollback, successful debit/reconstruction/highlight and unchanged unrelated state. Full add/build/upgrade/sell adapter, ordinary raid settlement durability, complete gameplay proofs and fresh APK/AAB remain open. Existing owner EXE and Android artifacts remain unchanged.

## 2026-09-11 14:58 - actual town repair buttons and practice integration PASS

Extended Assets/Editor/OwnedTownMovePlayProof.cs after real castle-to-final-raid-to-town/revisit: invoke actual Repair more and Repair button listeners on the active panel, resolve the selected instance and real catalog quote, require an enabled marker renderer/material, fund only the isolated memory fixture, inject a failed save and assert unchanged scene/property/wallet/durable bytes, then retry. Successful repair reload preserves the carried hero, exact material debit, repaired runtime HP=1, all unrelated structure records, and valid town navigation. Existing practice proof continues from that repaired town and passes win/save-refusal/retry, defeat without ordinary hero death, and abandon/return; actor-only kill isolation remains green. Main player save bytes are checked unchanged at the end.

Authoritative evidence: Builds/owned-town-repair-buttons-play.log and wrapper terminal VERDICT=PASS; markers OWNED_TOWN_REPAIR_BUTTONS_OK, OWNED_TOWN_REAL_PLAY_OK, OWNED_TOWN_HERO_CARRY_OK, PRACTICE_KILL_ISOLATION_OK, OWNED_TOWN_PRACTICE_PLAY_OK, OWNED_TOWN_MOVE_PLAY_OK. Unity terminal; no compiler errors observed. This proves programmatic invocation of actual UI listeners in Play mode, not physical touch input or visual marker positioning on device. Repair survival here is live scene reentry; cold-save reload is covered separately by OwnedTownRepairPaymentProof. Actual final-raid victory settlement remains explicitly uninvoked by this route test.

Next substantial scope: complete the approved owned-town build/upgrade/sell adapter using existing build interfaces (current design only relocates captured towers), and implement ordinary victory settlement recovery across the mapped stores. Then run a settled full regression candidate, Android channel checks, and required artifact/content build chains. No goal completion claim; no new executable/APK/AAB produced by this check.

## 2026-09-11 15:05 - construction save contract and atomic payment foundation

Inspection confirmed the existing owned layout validator/importer allows exactly the original221 inherited identities and unchanged levels; it cannot currently add/upgrade/sell. Added OwnedBaseConstruction detached add, one-level upgrade, and retire planners with expected revision and expected level checks. New construction starts at level1; upgrades require repaired condition and catalog maximum supplied by the gameplay authority. Selling retains a retired=true tombstone with original identity/level/pose; old saves default false. Clone and structural validation preserve this distinction; repairs/moves cannot revive a sold record. The existing v1 snapshot validator explicitly refuses tombstones until construction import is implemented, preventing silent resurrection. No player-facing construction doorway is enabled by this foundation.

GameStateService.TryCommitOwnedBaseConstruction commits an adapter-validated next revision with either an exact debit or a storage-limited refund in one save. It rejects stale/repeated confirmation, invalid quotes, insufficient funds and integer overflow; failure restores property and wallet, and notifications occur only after success. Catalog/plot/nav/timer validation remains the gameplay adapter responsibility. Coins and story BaseLayout are untouched. This is not yet a complete gameplay build adapter or construction timer implementation.

Evidence: Builds/owned-base-construction-payment.log and runner terminalPASS, OWNED_BASE_CONSTRUCTION_OK, zero compiler errors. Proof exercises detached add/upgrade/retire, stale and duplicate refusal, maximum/damage upgrade refusal, legacy missing-field behavior, sold-record clone/JSON persistence, actual save-failure rollback, successful debit/reload, refund failure/retry/no double refund, cold-load tombstone and unchanged story layout/coins. Existing OwnedBaseStateRegression also passes. Earlier pure-contract-only run preserved in owned-base-construction-contract.log. New proof is not yet registered in full DataRegression; register when gameplay adapter lands. Full regression and Android candidate remain pending.

Next implementation dependency: version the shared snapshot rules and importer for retained retired identities, new grid structures, and legal upgraded levels; reuse StructureFactory/BaseLayoutLoader spawning and tier authority, preserving fixed perimeter geometry. Then route existing build controls and upgrade timers to the owned-town transaction with unique ownership-aware job keys, validate navigation before charging, and prove add/upgrade/sell/reentry/practice. Ordinary victory settlement recovery remains separate unfinished release work. Goal remains active.

## 2026-09-11 15:16 - versioned construction replay in town and practice

OwnedTownLayoutSnapshot now emits owned-town-layout-v2 and continues validating unchanged v1 snapshots. V2 retains the complete inherited census, supports sold editable-tower tombstones and legal inherited tower levels, and validates additional ground-grid DefenseTower/WallSegment structures using the existing measured claim footprint, catalog ceiling, plot bounds and standing-structure intersections. Fixed perimeter/objective retirement and removal of captured identities refuse. Other factory families still need condition/gameplay adapters; this is partial construction-family coverage, not the full build goal.

BaseLayoutLoader.Spawn now delegates to shared SpawnForLayout while retaining its ordinary loaded set and story hooks. Snapshot import uses that factory path without story timer/vendor replay, stages additional bodies before inherited mutation, keeps a detached inactive occupancy helper (does not take the global PlacementGrid singleton), attaches OwnedTownPlacedIdentity, applies inherited upgrade stats through existing ApplyTierStats, restores damage and disables sold captured objects. OwnedTownScenePose.TryResolveStructure handles both inherited and added identities. Failed staging removes its inactive construction root. Legacy inherited pose lookup remains supported for upgrade application. No construction timers/UI purchase entry enabled yet.

Live evidence: Builds/owned-town-construction-manifest-play.log, wrapper terminalPASS and zero compiler errors. OWNED_TOWN_CONSTRUCTION_IMPORT_OK proves one new archer tower, inherited tower level2 metadata, inactive sold tower, unchanged saved structure records, and navigable actual town; existing reentry/hero/repair failure-and-retry and practice win/loss/abandon proofs then pass against that edited build. Main player save unchanged. This tests reconstruction and existing tier-stat application, not numerical balance acceptance or physical build gestures. Earlier failed runs retained: candidate fixture first used an unset hero and then selected the central objective by its shared tower catalog ID; fixture corrected to explicit Knight and manifest movableTower identity.

That shared-ID finding exposed a real existing selector risk. Added manifest.IsEditableTower: the central objective is not an editable defense even though it uses tower_arcane_spire. OwnedTownPanel selection and first-repair eligibility now use manifest identity. Later repairs do not demand a surviving tower once the lesson is complete, and retired structures never appear as repair candidates. Added-structure repair highlighting uses validated grid bounds.

Focused evidence: Builds/owned-town-construction-rules-retry.log terminalPASS, OWNED_TOWN_CONSTRUCTION_RULES_OK, zero compiler errors: v1 compatibility, immutable objective/census, v2 tombstone acceptance/v1 refusal, maximum upgrade refusal, first repair when only the central objective stands, and later wall repair after all towers are sold. First rules run failed to compile because the capture helper lives in a different editor assembly; corrected to call the existing CatalogBootstrap.Register through reflection. Latest Play run precedes this small selector fix; focused rules proof covers the changed cases. No fresh UI capture required by label/geometry changes (none in this slice).

Next: wire the actual owned-town build controls, ownership-aware upgrade timer keys/completion and atomic debit/refund through the construction service; handle added-grid movement and remaining structure families. Register construction proofs in the full gate when the integrated slice settles. Ordinary victory settlement durability and fresh APK/AAB/content chains remain unfinished; no artifacts or store publication performed.

## 2026-09-11 - owned upgrade queue scheduling/completion integration PASS

Added Core.State.OwnedTownJobKey (canonical escaped property+instance identity, independent of grid cells; does not parse as a story PlacedUpgradeKey). BuildTimerService.TryStartOwnedTownUpgrade validates the real catalog next-level plan and shared snapshot, derives the existing upgrade price and duration/talent haste, stages a clone of the Builder channel, and saves queue+property revision+payment together through TryCommitOwnedBaseConstruction. Failed save restores the old channel as well as the transaction's property/wallet rollback. Duplicate jobs refuse. Timers-off uses the same atomic edit/payment path. Starting a timer does not grant the level or complete a tutorial action.

ObsidianQueueEngine.Resolve gained an optional beforeRemove callback; existing callers remain compatible. BuildTimerService uses it for owned upgrades and also checks before active/pending direct completion removal. OwnedTownUpgradeCompletion saves the level first while the job still exists; if saving fails the job remains retryable. If interrupted after level durability but before queue cleanup, replay recognizes the already-earned target level and does not increment revision again. The generic story completion applier/effect registry is bypassed for owned keys. Live stats update only in the owned town; an active practice match retains its frozen imported build.

Evidence: Builds/owned-town-upgrade-timer-play.log, wrapper terminalPASS, zero compiler errors, OWNED_TOWN_UPGRADE_TIMER_OK plus prior construction import/repair/reentry/navigation/practice markers. Actual live builder service proof covers failed scheduling (property/queue/wallet/durable bytes unchanged), one exact debit, unique property key, duplicate refusal, pending level, forced due job with failed completion save, successful level commit while the job remains, cold load and idempotent replay/cleanup. Existing practice win/loss/abandon and main player save preservation also pass after the upgrade.

Still not player-ready: hook existing build/upgrade controls into the owned service, present readable job labels/scaffolds, integrate owned-aware cancellation/refund and paid instant-finish failure semantics, and preserve instant-build convenience-token behavior atomically (new owned scheduler currently uses duration/talent haste but does not consume instant-build tokens). Do not expose the owned scheduler through generic paid shortcuts until those paths are handled; no production owned jobs created by this isolated proof. Additional structure-family adapters, build and sell controls, added-grid movement, ordinary raid settlement recovery, full regression registration and APK/AAB/content validation remain open. No goal completion or artifact publication.

## 2026-09-11 - owned upgrade cancellation PASS; paid finish validation running

BuildTimerService.TryCancelOwnedTownUpgrade stages removal and exact full JobCost refund in one TrySave, with complete queue/wallet rollback on failure. Generic CancelChannelJobWithRefund dispatches owned keys here; bare caller-refunds CancelChannelJob refuses owned keys. Active and pending jobs both supported; completed-level-before-queue-cleanup jobs refuse cancellation refunds. No production-storage clamp on cancellation: this returns paid resources. Negative/overflowing refund baskets refuse without deleting the job.

Verified Builds/owned-town-upgrade-cancel-play.log terminalPASS and zero compiler errors. Expanded live timer proof covers active and pending cancellation failure rollback, retry, exact paid basket, repeated-cancel refusal, cold reload, requeue and refusal after the earned level is durable. All existing town construction/repair/navigation/practice checks pass afterward.

Paid finish implementation now separates OwnedTownUpgradeCompletion.TryPlan and RefreshLive from natural TryCommit. TryInstantFinish routes owned keys through a staged Builder channel and TryCommitOwnedBaseConstruction so quoted crystals, earned level and job removal share one save. Durable-level-only cleanup never charges again. Added actual-price failure/retry/reload coverage to the live proof.

CURRENT RUN NOT VERIFIED: Builds/owned-town-upgrade-finish-play.log, wrapperPID54716 and UnityPID50580. Unity remains responsive/live but log stopped at package test-assembly loading after compilation; no test marker or terminal wrapper verdict yet. Last checked process CPU approximately9.9 seconds. No compiler errors observed so far, not a PASS claim. Do not edit C# or start a second Unity batch until this specific run is terminal; recheck both handles/log. Existing runner owns its timeout. No retry authorized by mere observation delay; wait for actual terminal state. Goal remains active and this is a verified wait, not a blocked goal.

Read-only next integration finding: BuildModeController.Enter still seeds story BaseLayout and Place uses BaseLayoutLoader; CommitLayout saves story layout regardless of scene. Owned-town build entry must be routed before these calls, not merely given a new palette button. Current owned placement/upgrade UI integration remains unfinished. Instant-build convenience tokens, readable job labels/scaffolds, additional structure families and added-grid movement remain open along with raid settlement recovery and APK/AAB release chains.

## 2026-09-11 - cloud owned-town preservation and recovery-intent isolation

The user-requested highest-reasoning document review is complete in docs/SKR_VISION_RECONCILIATION_2026-09-11.md. It is advisory, adds no release scope, and changes none of the four original documents. Previous turn classified as progress: source comparison disproved the proposed missing dungeon boss contract and produced a reconciled decision artifact. Release work resumed without another advisor review.

Actual api/game/save.js buildState now reserves pendingTownCapture and PendingTownCapture as local-only recovery fields. Existing ownedBase, retired construction records, inherited poses, new grid placements, repair supplies/receipts/milestones and owned queue keys remain in the cloud snapshot. Partial older payloads do not blank the owned town. api/game/load.js also removes either recovery-intent spelling from a copied response, so an older stored blob cannot export another device's unfinished capture intent; it does not modify the stored row or remove the earned ownedBase.

Evidence: node --test test/game.save.owned-town.test.js test/game.save.schema-version.test.js test/game.save.reset-epoch.test.js: 26/26 PASS, zero failures. Actual buildState mapping and mocked load handler exercised alongside existing reset/schema guards. No production database requests or deployment. This is backend source behavior, not proof of a deployed cloud round trip.

Unity paid-finish validation remains the same live UnityPID50580/wrapperPID54716, started15:32:29. Latest checked log still382411bytes lastwrite15:32:47; process handles alive with CPU increasing slowly, no harness marker. Existing25-minute runner owns timeout (approximately15:57:29); do not edit C# or relaunch until authoritative termination. Read-only tracing confirmed added-grid movement still calls inherited-only pose resolution and that the existing hero/build controls must route owned state before story seeding. Those are next integration changes, not fixed by this backend slice.

## 2026-09-11 16:03 - added-grid movement and paid-finish integration PASS

The old paid-finish run terminated at its configured timeout: owned-town-upgrade-finish-play-runner.log VERDICT=FAIL/HARNESS_TIMEOUT. Both exact handles disappeared before C# edits. No gameplay verdict was inferred from that stalled run.

Implemented added-grid tower relocation through the existing design service and move operation. OwnedBaseConstruction.TryMoveGrid produces a detached revision preserving identity, level, condition and orientation; same-cell, stale, retired and inherited-target requests refuse. OwnedTownDesignService and OwnedTownPanel resolve new or inherited bodies with TryResolveStructure. New bodies snap to their imported town grid, then pass the existing collision/shared-snapshot/live-navigation checks. Only after a successful save does OwnedTownMoveOperation update PlacedStructure gridCell and occupancy; failed previews restore the position while leaving metadata and durable state unchanged. This extends current movement, not the still-missing full construction UI adapter.

Extended actual town Play proof with a new tower's real navigable move. It explicitly requires reaching an injected provider write failure (counted), verifies scene/property/metadata/durable rollback, retries successfully, cold-loads the moved record, refuses same-cell revision churn, and checks runtime grid metadata after scene reentry. Story layout and wallet remain unchanged. The subsequent existing repair, natural-upgrade/cancellation and paid-finish proof plus practice win/loss/abandon all executed in the same successful run.

Evidence: Builds/owned-town-grid-move-play.log and runner terminal VERDICT=PASS, OWNED_TOWN_GRID_MOVE_OK, OWNED_TOWN_UPGRADE_TIMER_OK, OWNED_TOWN_MOVE_PLAY_OK; zero C# compiler errors. Paid finish now has runtime evidence for actual quoted crystal price, failed-save rollback, successful exact debit/level/job removal, repeat refusal and cold reload. Earlier canceled-upgrade and construction proofs remain valid; this run adds coverage instead of treating the earlier timeout as a pass. No new UI layout/copy was changed by this movement slice; panel route delegates to the tested service, but physical gestures are not certified here.

Startup diagnosis/recovery on the new run: Unity22912/wrapper51160 initially could not start its editor-specific licensing client. Local Unity.Licensing.Client.log proved old client45932 (parent50580, exact pinned-editor binary) closed its pipes after the timed-out editor died but retained its global mutex; newly launched17000 exited with 'Failed to acquire global mutex'. Stopped only that verified orphan, started the same pinned editor-specific service (PID15328, --namedPipe Unity-LicenseClient-Elden-6000.4.8). Current Unity resumed, compiled and completed its tests without restarting the editor. Hub and its shared client were not changed. Unity22912 and wrapper51160 are now terminal. This explains the new run's licensing obstruction; it does not establish the original assembly-loading stall's cause.

Next required work remains: route existing build/upgrade/sell controls to the owned-town authority before story seeding/placement, preserve instant-build convenience-token semantics atomically (including queued cancellation), add readable owned-job controls, complete supported construction-family behavior, settle ordinary raid victory recovery, register the new focused proofs in full DataRegression, and build/validate APK+AAB plus required content and owner device checks. Goal remains active; current EXE/Android artifacts were not replaced, and no deployment/publication occurred.

## 2026-09-11 16:15 - existing upgrade page connected; instant-build tokens and UI door PASS

Previous goal turn classified as progress: verified movement/paid finish and resolved a measured licensing obstruction. This turn extends the player-facing owned upgrade path while keeping the full release objective intact. User asked how close release is; answered that integration remains before final packaging, with no evidence-based promise of release today.

PlacedStructureUpgradeService now resolves owned-town job identities against the current OwnedBase, reads the saved per-instance level, and delegates owned starts to TryStartOwnedTownUpgrade. Wrong-town/missing identities refuse. BuildingUpgradeVM uses that shared resolver and LevelOfKey, so the existing enhancement page shows the owned catalog preview and actual level instead of falling through to unknown or reading story BaseLayout. Story placement keys retain their original path. OwnedTownPanel's Upgrade button opens the existing registered page for the selected owned instance. Full generic BuildModeController entry/place/sell routing is still unfinished; this new doorway does not claim to make that generic path safe.

Paid instant-build convenience support is now atomic on owned upgrades. ConvenienceRedeemer supplies a detached token-consumption plan using the existing primary/legacy inventory keys. BuildJobData has additive optional instantBuildTokenKey (absent old jobs remain null). Owned scheduling stages inventory+queue+payment/property in one save and restores inventory on failure or rejected enqueue. An unfinished queued job retains the exact token receipt; cancellation validates its kind and credits one token with its full resource refund in the same save, restoring all on failure. Already-completed upgrades cannot also claim cancellation refunds. A zero-duration job without time to skip does not consume a token.

Evidence: Builds/owned-town-upgrade-page-token-play.log terminalPASS, no C# compiler errors. Actual existing BuildingUpgradeVM.UpgradeNext exercised failure/retry using the owned identity and real catalog level; existing exact-payment/durable-completion/cancellation/paid-finish checks continue. New OWNED_TOWN_INSTANT_BUILD_TOKEN_OK covers legacy-key consumption, queued zero-duration receipt, injected enqueue save failure, cold reload, cancellation failure/retry/exact resource+token refund, duplicate refusal, and immediate completion after a crew frees. Main player save untouched.

UI: paired Select next tower and Upgrade in the existing selection row, keeping the same row count. Added ownedTown.upgrade (35 owned-town keys total) across English and nine draft locale dictionaries in both mirrors. Extended capture semantics to require select/upgrade/move actions in design/ready states. First UI capture passed but visual inspection found other labels crowded against ornamental borders; applied the same18..28 autosizing to town action labels. Final Builds/owned-town-upgrade-door-readable-ui.log terminalPASS:24frames, UI_GEOMETRY_OK24, UI_TOUCH_OK24/24, UI_GLYPH_OK24/24 with144labels. Opened and visually inspected final1920x1080 design capture; earlier2670x1200 ready capture also inspected. No native-language acceptance or fresh full localization suite claimed for the additional simple label.

Final integrated runtime proof: Builds/owned-town-upgrade-door-play.log / runner terminalPASS, zero C# compiler errors. OWNED_TOWN_UPGRADE_DOOR_OK programmatically invokes the actual town Upgrade button, verifies the existing panel opens with the selected owned key/current level, then closes it and continues grid movement, repair, upgrade/token/paid-finish and practice proofs. All pass, main player save unchanged. Unity21328/wrapper3984 are terminal. This is actual UI listener integration, not physical touch certification.

Remaining release blockers: full owned add/build/sell controls and generic builder state routing; new-building timer/visual integration and remaining construction-family adapters; ordinary final-victory recovery across mapped stores; full raid gameplay/navigation route and settled regression; APK/AAB/channel/content validation and device acceptance. Existing owner EXE and Android artifacts remain unchanged. No store or backend deployment; goal active.

## 2026-09-11 16:31 - owned sale transaction and confirmation integrated

Previous goal turn was a status-only reply (no progress). Revalidated that Unity was closed and continued the existing construction integration. Added OwnedTownConstructionService.TryQuoteSale/TrySell and an actual Sell button alongside selection/Upgrade. The confirmation shows the shared invested-cost refund and explains storage limits; successful feedback reports the actual credited amount. The existing private BuildModeController refund calculation now delegates to a public itemId/level overload, preserving build-plus-upgrades, halving/rounding and Salvager behavior. The private component overload remains available for existing reflection regressions.

Sale validates the owned snapshot before committing, protects the fitted perimeter/objective, rejects stale confirmations and active/pending owned upgrades, and refuses during an unresolved move or outside the owned scene. Finish or cancel the upgrade before sale; this preserves its existing atomic cancellation/refund rather than splitting sale across multiple writes. Retired identity plus storage-limited refund share TryCommitOwnedBaseConstruction. Only after durability does the service free the added structure's own grid and deactivate its body. Captured template objects are retained for identity replay. If all selectable towers are gone, the old selection highlight is hidden. This is the owned-panel sale route; generic BuildModeController selection/sale routing remains unfinished.

Runtime evidence: Builds/owned-town-sale-ui-play.log and runner terminal VERDICT=PASS; OWNED_TOWN_SALE_OK and OWNED_TOWN_MOVE_PLAY_OK. The actual Sell/Keep/confirm listeners run against an upgraded added tower. Proof covers fixed-structure, pending-upgrade and stale refusal; injected write failure actually reached with unchanged live body/property/wallet/durable bytes; confirmation cancellation; successful exact storage-limited refund; retained retired record; repeat refusal; cold-load property/resources with unchanged story layout and builder queue; and navigation after obstacle removal. Existing real town movement/repair/upgrades/tokens and practice win/loss/abandon continue passing. Main player save unchanged. This does not certify physical touches or an owner device. Earlier owned-town-sale-play.log was a compiler failure from the Village/Core ResourceCost name collision; corrected using an explicit CoreCost alias before the successful run.

UI evidence: final Builds/owned-town-sale-inset-ui.log and runner terminal VERDICT=PASS, zero compiler errors, 27 frames, geometry27/27, touch27/27 and glyph27/27 (165 labels). Extended captures with the same production confirmation builder and a four-resource quote, and require the new Sell button. First capture correctly found "Keep structure" truncated; shortened English to "Keep". Updated the expected frame count from24 to27. A subsequent visual inspection found town captions crowding decorative borders despite passing glyph counts; added horizontal label padding and reran. Inspected final design captures at1920x1080 and2670x1200 plus the confirmation at1920x1080. Added four sale keys across English and nine draft locale mirrors (39 owned-town keys); native-language and full localization acceptance remain pending. Initial capture runner misclassified the UI failure as LICENSE_ERROR; the editor actually rendered27 frames and threw its UI validation exception, so no licensing process intervention was made.

Next: complete new-building placement/timers/visuals and route the generic builder to owned state before story seeding or mutation. Ordinary final-victory recovery, complete raid route, settled full regression, APK/AAB/channel/content validation and owner device checks remain required. No fresh EXE/APK/AAB, production write, store publication or goal-completion claim. Unity batches are terminal.

## 2026-09-11 16:43 - new-construction save, timer and scaffold replay PASS

Previous goal turn was progress (owned selling integrated and verified). Current inspection confirmed generic builder Enter/Place/selection still targets story BaseLayout. Before routing placement, implemented the missing durable unfinished-building state and timer lifecycle. No generic builder entry or placement has been advertised as complete.

OwnedBaseStructure adds constructionPending (old records default false), preserved by cloning and validation. Pending construction must be a new nonretired level-one grid structure at full condition. OwnedBaseConstruction supplies detached begin/complete transitions; upgrades, moves and repairs refuse unfinished construction, and cancellation retirement clears the flag. Shared snapshots now emit owned-town-layout-v3 and still accept completed v2 construction and v1 captured layouts. V2 explicitly refuses unfinished structures rather than interpreting them as complete.

BuildTimerService.TryStartOwnedTownBuild is an internal placement-adapter seam: its caller must validate catalog/plot/navigation and the displayed quote before calling. It validates the proposed snapshot and commits property, exact passed price, queue, optional free-build ledger consumption and exact instant-build token receipt in one save, restoring all staged state on failure. Duration uses existing catalog cost tier, build curve, grace and talent haste. Timers-off writes a completed record; timed jobs retain the pending flag until durable completion. Existing owned completion/paid-finish routing now recognizes Build jobs: completion clears the flag before queue removal, and the cleared flag is the replay receipt. Owned cancellation now handles Build jobs by committing a retained retired identity, queue removal, full paid refund and optional token refund together. Only after saving does it deactivate any live cancelled body and free its own grid. Free-build consumption remains used after cancellation, matching the nonrenewable placement allowance.

Town reconstruction attaches the existing UnderConstructionVisual to pending new bodies with the owned job key, after applying condition. The scaffold disables combat and displays the actual timer/worker/aura. A saved pending record does not self-reveal if the queue service is absent. Practice import requests a frozen scaffold, which ignores live timer completion and keeps its snapshot combat gate; that new frozen-pending practice case is not yet exercised end-to-end (current practice proof runs after construction completion). Existing actual practice win/loss/abandon remains green. Pending structures are excluded from ordinary tower edit selection until completed; readable owned queue controls and placement integration remain next work.

Evidence: Builds/owned-town-new-build-play.log, owned-town-new-build-replay-play.log and final owned-town-new-build-visual-play.log all terminal VERDICT=PASS, zero C# errors. OWNED_TOWN_NEW_BUILD_OK covers actual scheduling write failure reached, unchanged property/payment/queue rollback, exact successful debit, cold load still pending, actual SceneRouter reentry with scaffold and disabled DefenseTower, forced completion save failure retaining pending/job, retry enabling combat, idempotent completion receipt, completed cold load, cancellation save failure/retry/full refund/tombstone/story isolation. Expanded run proves v2 rejects pending but accepts completed layouts, and failed/successful instant free construction preserves/consumes both the free-build ledger and exact legacy token correctly. Main player save unchanged. No claim of actual drag/place or navigation-before-charge acceptance from this test: it directly exercises the adapter seam at an already validated previously used cell.

Visual: ScreenCapture.CaptureScreenshot produced no PNG under this batch configuration, so it was replaced with the existing render-request/RenderTexture readback pattern. Final OWNED_TOWN_CONSTRUCTION_CAPTURE_OK writes Builds/owned-town-construction-live.png (1920x1080). Opened and inspected: the real town contains the dimmed wooden tower, readable2:21 countdown, construction aura and worker. The proof temporarily frames the real camera around the tower and restores it afterward; this is a world/scaffold capture, not a phone HUD/touch check. No new panel copy/layout changed in this slice, and no full localization claim made.

Remaining: wire the existing ghost/placement controls to a reversible owned staging/navigation operation and this scheduler; make all generic selection/move/upgrade/sell and queue controls use owned identities; complete supported construction-family behavior and frozen pending-practice proof. Then ordinary final-victory recovery, full raid route/regression, APK/AAB/channel/content builds and owner device acceptance. All Unity/wrapper handles from this slice are terminal. Existing EXE/APK/AAB unchanged; goal active, no publication or deployment.

## 2026-09-11 16:57 - existing builder placement connected to owned transactions

Previous goal turn was progress (new-build durability/timers/scaffold proven). Added the owned-town Build doorway, paired with reentry/practice without increasing the design panel row count. It opens the existing BuildModeController and hides the town panel/highlight. Owned Enter selects the imported construction grid and skips story seeding/census; owned Place delegates before the old story placement path; owned Exit cancels unsaved construction previews and does not write story BaseLayout. The inactive imported grid retains its singleton isolation; its explicitly requested outline renders under the active construction root.

OwnedTownConstructionService.TryBeginBuild creates a unique identity and validates the detached snapshot, catalog-supported footprint, grid occupancy and level NavMesh support. It quotes the existing free-build/softcap price before spawning the preview, derives the existing first-ever/onboarding/pallet grace, and uses BaseLayoutLoader.SpawnForLayout. OwnedTownBuildOperation disables preview tower combat, waits for actual navigation updates, validates town routes, refuses changed property state, then invokes the atomic builder scheduler. Save failure or cancellation removes the preview and frees its grid; success retains the body and attaches the actual timed scaffold. Moves and sales refuse while a build preview is unresolved. EverBuiltStructureIds is now staged and saved with the construction payment/queue/free-build/token state, with rollback on failure.

The generic completed-tower selection route now exits build mode and opens OwnedTownPanel for the stable owned identity, so its move/upgrade/sell actions use the owned services. Inherited editable towers receive PlacedStructure metadata even at their original level, enabling the existing tap-selection machinery without rewriting fitted geometry. Unfinished or non-tower selections refuse rather than silently choosing another completed tower. This selection surface still needs supported wall and job-control integration; this is not a claim that every catalog family is ready for the owned scene.

Runtime evidence: Builds/owned-town-placement-play.log, owned-town-placement-exit-play.log and final owned-town-placement-selection-play.log all terminal VERDICT=PASS, zero compiler errors. OWNED_TOWN_PLACEMENT_OK exercises the actual town Build listener, existing ArmById palette path, and the real private Place commit using a known valid target. It proves owned grid selection, navigation-before-save, an injected write failure actually reached with property/wallet/history/grid rollback, retry adding exactly one owned record, cancellation on Exit, occupied repeat refusal, later job cancellation freeing the live grid, and unchanged story BaseLayout. Final extension checks unfinished selection refuses and completed inherited selection opens the exact owned identity. The full existing repair/move/sale/upgrade/token/new-build and practice win/loss/abandon sequence also passes; main player save unchanged. Physical pointer/drop/confirm gestures and the complete generic ghost-validity path are not proved by invoking Place directly.

UI evidence: Builds/owned-town-build-door-ui.log terminal VERDICT=PASS, zero compiler errors, 27 frames, geometry/touch/glyph27/27 (171 labels). Capture semantics require Build in design/ready. Opened and inspected OwnedTownDesign_1920x1080.png and OwnedTownReady_2670x1200.png. Build is paired with save/reentry or practice. Added ownedTown.build in English and nine draft locale mirrors (40 owned-town keys); no full localization/native-language acceptance claimed. Final selection guard changes no panel geometry and compiled in the last Play run.

Scope recheck: reread WO-1705's owner-established outcome and acceptance section. The mandatory outcome remains actual final-tier victory, damaged durable property, successful repair/design/save/reentry and shared-build no-wager AI practice, followed by Android/device acceptance. Future live player competition/certification is explicitly later. Keep remaining catalog work grounded in that approved scope and the choices exposed by the owned builder, rather than expanding unrelated castle systems.

Next: finish supported palette/selection/job controls and prove the complete ghost/confirm route plus frozen unfinished-practice behavior. Then resolve final-victory recovery and real outcome-to-town integration, run the settled full regression/artifact/content gates, and produce fresh APK/AAB for owner device acceptance. No new release artifacts or deployment in this slice. All Unity/wrapper handles are terminal; goal remains active.

## 2026-09-11 17:13 - placement input and concurrent practice construction PASS

Previous goal turn made progress: corrected the new Play proof's definite-assignment compile error and launched its retry. This turn finished the frozen-construction investigation and verification. The earlier placement-input run and the final run below exercise injected screen-space input through the actual ray/ghost validity, world drop and real Place button listener. Dropping alone does not charge; confirmation saves exactly one owned structure. This is stronger than private Place invocation, but is not physical touch/device acceptance.

OwnedTownPracticeSession now retains a detached entry property and validates capture identity, template/schema and a nondecreasing revision when saving a completed match. It merges only the lesson milestone into the current property, preserving legitimate builder completions during practice instead of trapping Return on exact entry-JSON mismatch. Different capture identities and older revisions remain refused. The match snapshot itself remains detached and unchanged.

The live test exposed a second mismatch: the frozen scaffold remained attached, but UnderConstructionVisual.IsUnderConstruction answered from the owner's live queue, becoming false when that timer ended. The query now respects the frozen practice flag and saved pending owned construction before consulting the ordinary queue. This keeps construction-dependent effects consistent with the frozen opponent. The first combined assertion was split to locate this exact mismatch; no claim that the visual itself disappeared.

Final runtime evidence: Builds/owned-town-frozen-gate-play.log and runner terminal VERDICT=PASS, zero compiler errors. OWNED_TOWN_PLACEMENT_INPUT_OK and OWNED_TOWN_FROZEN_CONSTRUCTION_PRACTICE_OK present. The latter starts a real owned construction job, imports practice, completes the owner's timer, verifies the practice scaffold and disabled tower remain, exercises failed result-save/retry, returns through the actual controller and cold-loads both completed construction and the practice milestone. Existing repair/move/upgrade/sale/new-build and practice win/loss/abandon checks also pass; main save unchanged.

Focused state evidence: Builds/owned-town-concurrent-state.log and runner terminal VERDICT=PASS, OWNED_BASE_STATE_OK, zero compiler errors. Includes concurrent structure preservation, different-capture refusal and failed-save/retry behavior. No new UI geometry was changed in this slice. All Unity/wrapper handles are terminal.

Remaining: supported palette/selection/job controls; actual final-raid victory and interrupted settlement recovery; full release regression; fresh EXE/APK/AAB, artifact/channel/remote-content checks and owner device acceptance. No new build artifacts, production changes or store publication. The release goal remains active.

## 2026-09-11 - full regression in flight after practice fixes

Previous goal turn was progress: frozen construction/practice fixes and focused proofs passed. Current source review confirms the owned builder still exposes the generic castle catalog while its snapshot accepts ground DefenseTower/WallSegment construction, and completed-wall/pending-job selection needs integration. No catalog choices were removed in this review.

Started the full suite to identify cross-system failures before further edits. Initial launch used the wrong namespace (DeNelle.Editor.Regression.DataRegression), terminated with executeMethod class not found, and ran no tests. Corrected to the source-declared DeNelle.Editor.DataRegression.RunAll. Active run: Builds/release-owned-town-full-retry.log, wrapper PID54224, Unity PID51300; both confirmed live at this update. No terminal verdict yet. Do not edit C# or launch another Unity until this exact run is terminal. Observation gaps are not failures.

Source audit also found OwnedBaseConstructionProof and OwnedTownConstructionRulesProof are not registered in DataRegression. Register these existing focused proofs after the active batch finishes, ensuring thrown assertions count as failures rather than being swallowed by Guard.Try. Inspect any full-suite failures, resolve them without weakening checks, and rerun only as warranted. No fresh artifacts or release-completion claim.

## 2026-09-11 17:23 - full gate findings corrected; expanded rerun active

The preceding run is terminal: Builds/release-owned-town-full-retry.log reports REGRESSION_FAIL, 508/510 suites green, zero skipped. Two findings: ResourceAuthorityRegression's substring `s.Stone =` falsely matched the end of `Resources.Stone =`; generated Unity localization tables lacked six canonical owned-town labels (build, keepStructure, saleComplete, saleQuote, sell, upgrade).

Changed the retired-balance scan to match complete direct `_state`/`s` receivers with identifier boundaries, retaining the prohibition on a second direct Stone balance. Added classifier checks for direct-state positives and legitimate Resources negatives. No resource authority or payout was weakened. Registered OwnedBaseConstructionProof and OwnedTownConstructionRulesProof in DataRegression with explicit exception-to-failure handling; failures cannot vanish through Guard.Try.

Regenerated localization using OwnedTownReleaseContractProof.RunLocalized. Builds/release-town-localization-refresh.log and runner terminal PASS, zero compiler errors, OWNED_TOWN_RELEASE_CONTRACT_OK and LOCALIZATION_REGRESSION_OK 9/9, including exact canonical/table parity. This is localization data validation, not native-speaker approval.

Expanded full rerun is now active: Builds/release-town-registered-full.log, wrapper PID40912. Verify its Unity handle and terminal marker before editing C# or launching another Unity. The corrected full gate is not yet proven green. Packaging preflight also confirmed keystore.properties exists without reading credentials, ample C/D disk space, and that the August mainnet/store warnings are superseded by publishing/SUBMISSION_BLOCKERS_CLEARED_2026-08-25.md and current source. Signing correctness and fresh artifacts remain unverified. No APK/AAB generated or published.

## 2026-09-11 17:25 - expanded full regression PASS

Builds/release-town-registered-full.log now reports REGRESSION_OK 512/512 suites -- 512 green, zero red, zero skipped. Runner terminal VERDICT=PASS; zero compiler errors. Wrapper40912 and Unity42468 are gone. This supersedes the historical 508/510 and 506/509 failures for the current source. Both newly registered construction proofs ran as part of the full gate.

This proves the registered suite scope, not the unimplemented final-victory recovery or physical/device gameplay. No need to repeat this full run without subsequent changes or new concerns. Next work remains integration of exposed owned build controls, actual final-victory-to-town and interruption recovery, followed by fresh platform artifacts/content checks and owner testing. No artifact or store-publication claim.

## 2026-09-11 17:31 - pending construction selection and localized amounts PASS

Owned BuildMode selection now exits placement and opens the existing BuildingUpgrade page for the exact owned job when the selected structure is still under construction. Previously it rejected selection; it no longer silently falls through to a different completed tower. Live proof verifies the page is open, BuildingId is the selected owned job key, and UnderConstruction is true. This proves page/countdown access, not yet every queue cancellation/skip gesture.

The first Play run exposed a sale confirmation regression after table regeneration: fetching a formatted localization string without arguments and subsequently calling string.Format did not preserve the expected refund text with the live provider. OwnedTownPanel now calls LocalText.Format with arguments directly for repairPayment, saleQuote and saleComplete. The sale test preserves its amount assertion and now reports actual text on failure. No quote/refund math changed.

Builds/owned-town-pending-format-play.log and runner terminal PASS, zero compiler errors, OWNED_TOWN_PENDING_SELECTION_OK plus full OWNED_TOWN_MOVE_PLAY_OK. Existing sale, placement, repair, timer, frozen-practice and return checks pass. Builds/owned-town-formatted-ui.log terminal PASS, 27 frames, geometry/touch/glyph27/27, 171 labels, zero compiler errors. Inspected OwnedTownSaleConfirm_1920x1080.png and OwnedTownRepair_1920x1080.png: formatted amounts visible, text and buttons fit. All Unity/wrapper handles are terminal.

Full 512/512 evidence predates these small selection/formatting changes; focused Play and UI checks validate this slice. Player-built wall editing, supported palette integration, final victory/recovery and fresh artifacts remain outstanding. No APK/AAB or publication in this slice.

## 2026-09-11 17:37 - player-built wall editing PASS

Added IsEditableStructure alongside the existing tower-only first-repair predicate. Completed player-built WallSegment records now participate in town selection, movement and sale; inherited perimeter records remain fixed. BuildMode selection uses this shared predicate, and the move service admits supported walls through the existing grid/snapshot/collision/live-navigation/save path. The existing nextTower localization key now displays Next structure (all ten canonical mirrors and translation source updated); no key was renamed or removed. The first repair lesson still specifically restores an editable defense tower.

Builds/owned-town-wall-localization.log terminal PASS, zero compiler errors, localization9/9. Builds/owned-town-wall-edit-play.log terminal PASS, zero compiler errors. OWNED_TOWN_WALL_EDIT_OK proves a real wall build through the construction service, completion, selection through BuildMode into the exact panel identity, a navigation-validated live move and saved revision, sale and cold reload, unchanged story layout and protected fitted perimeter. Existing pending selection, tower/repair/placement/payment and frozen-practice checks also pass. This slice does not separately prove wall upgrade UI or every physical input gesture.

Builds/owned-town-wall-selection-ui.log terminal PASS: 27 frames; geometry/touch/glyph27/27, 171 labels, zero compiler errors. Inspected OwnedTownDesign_1920x1080.png; Next structure fits its button with adjacent Upgrade/Sell and the remaining controls. All Unity/wrapper handles are terminal. Remaining: supported palette/ghost alignment and queue gesture coverage; actual final victory and interrupted settlement; settled release gates, fresh APK/AAB/content and device acceptance. Goal active; no publication.

## 2026-09-11 - owned palette and preview rule alignment PASS

OwnedTownLayoutSnapshot exposes the supported construction predicate (ground DefenseTower/WallSegment, excluding required WallWalk seating) and retains the shared grid/footprint/plot validator. The default BuildPaletteVM receives an optional placement-reason provider: in the owned town, unsupported castle rows are visible but locked with an explanation; existing unlock-hidden rows remain hidden until earned. Both ArmById and direct Arm enforce the same location predicate. Other scenes and injected test VMs retain their prior behavior. No castle catalog rows were removed.

The actual IsValidPlacement ghost path now checks TryGridBounds before affordability, passing the same item/cell/yaw/seat/wallMounted data used by Place. A refusal is visible through the existing ghost/toast reason text. This inexpensive check avoids cloning/validating the entire captured census each frame; final commit still validates the complete snapshot and live navigation before saving.

Builds/owned-town-palette-rules-play.log emits OWNED_TOWN_PALETTE_RULES_OK and OWNED_TOWN_MOVE_PLAY_OK. The runtime test enumerates all build verbs, requires both supported and blocked cards, checks unsupported cards are locked with reasons and cannot arm, preserves owned state, verifies shared plot refusal, and retains the actual valid screen-space ghost/drop/confirm path. Existing tower, wall, pending selection, payment, cold-load and practice checks pass. This does not prove the outside-plot physical pointer gesture or render the new unavailable-card copy; final palette visual review remains required. Full512/512 predates these integration changes.

Next priority: actual final-raid victory-to-town outcome and interruption-safe settlement, then settled release/artifact/content gates and owner device acceptance. No fresh APK/AAB or publishing in this slice.

## 2026-09-11 - final route and garrison direction

FinalRaidRouteProof now passes in a fresh editor run: `FINAL_RAID_ROUTE_OK` confirms the saved Iron Bastion entry -> staging -> actual snapped boss -> objective approach paths and final-tier spawner. This is navigation/scene evidence; it explicitly does not prove runtime victory, reward settlement or return.

The owner raised the future two-barracks direction. The chosen product shape is one shared troop inventory with two assignments: a castle expedition army for raids and an owned-town garrison for defense/practice. The town barracks will not create a second training economy or duplicate units. It becomes necessary when player-versus-player town attacks arrive; the current release remains AI practice only and keeps the existing shared-build contract. Do not expand this release into live PvP or a second army ledger.

OwnedTownReconstructionProof also completed its runtime-independent reconstruction checks and emitted `OWNED_TOWN_RECONSTRUCTION_OK`: 210 walls, 10 towers, 1 spire, exact saved damage/poses, friendly structures, no garrison spawner, story save unchanged. Its runner has not yet emitted the terminal wrapper verdict; do not launch another Unity batch until that handle is terminal.
## 2026-09-11 - release candidate artifact gates

The changed source snapshot has now passed the final editor regression and both Android artifact chains. The fresh evidence is:

- `Builds/release-regression-final.log`: `REGRESSION_OK 512/512 suites -- 512 green, 0 red, 0 skipped`.
- `Builds/overnight-apk-status.txt`: `APK_OK`, `R2_PARITY_OK targets=Android,StandaloneWindows64,WebGL objects=225`, and `APK_DONE`.
- APK: `Builds/Android/DefendersOfTheRealm.apk`, 466,347,097 bytes; SHA-256 `F5A41A8FBC117DEB3581D575D0B46665003C82FBE18CFB9C323885887442D61A`; `apksigner verify` passed with APK Signature Scheme v2 and one signer.
- `Builds/aab-status.txt`: fresh `AAB_OK`, `AAB_SIGNING_OK`, `R2_PARITY_OK`, `AAB_SIZE_OK 451692572 (48307428 under 500000000)`, and `AAB_DONE`.
- AAB: `Builds/Android/EchoesOfElarion-GooglePlay.aab`, 455,117,262 bytes; SHA-256 `072A3CB6ACEF82EABAF349CECEF6B67D4AB8AEF67A1DEA3FE8F9586BBA8D6025`. `jarsigner -verify` returned exit 0; the release keystore is the stable `dotr` signer.

The AAB Unity wrapper recorded `AAB_BUILD_MARKER_ABSENT` because Unity returned exit 8 during its batch-mode shutdown/size-dialog path even though the build reported `Build Successful` and the wrapper independently accepted the fresh artifact, signature, size, and parity gates. Keep this provenance visible; do not describe it as a clean Unity marker pass.

Remaining acceptance gates are owner/device checks: install the fresh APK on the target Android device, exercise final-tier raid victory through damaged Iron Bastion, repair/design/save/re-enter, and run the shared-build AI practice return loop. The automated route proof still does not execute a complete runtime victory settlement, and no store publication has occurred.

## 2026-09-11 19:00 - final source fix, rebuilt artifacts, and Seeker install

The final source change addresses the observed Seeker raid behavior: turning Rally off now clears the persisted `TroopRally.Point` and hides the rally flag, allowing idle Footmen to resume their spire/objective push. The captured issue showed `Rally ON` and an active rally flag; it was command state, not a frozen process. `Builds/rally-clear-compile-final.log` is clean and `Builds/rally-clear-regression-final.log` reports `REGRESSION_OK 512/512 suites -- 512 green, 0 red, 0 skipped`.

The stale `MainCastle_Hall` prefab references were removed through `MissingPrefabInstanceCleaner.CleanMainCastle`: 37 missing instances were removed, the three affected GUID counts are now zero, and neither final Android build log contains a missing-prefab warning.

Fresh final artifacts from this source state:

- APK: `Builds/Android/DefendersOfTheRealm.apk`, 466,351,625 bytes; SHA-256 `08A3E90D3D19530FF8D632F0027F507801B94B13DF0F5F41DDD1F04DB013F425`. The APK wrapper has a fresh Unity `[AndroidBuild] SUCCEEDED` marker, R2 parity for 225 objects, and release signing verification.
- AAB: `Builds/Android/EchoesOfElarion-GooglePlay.aab`, 455,113,849 bytes; SHA-256 `DB86E7F0F766D549F127E8A107F8CBA5D9836DD359C65600DF7D1ED4FE824DB7`. The wrapper passed `AAB_OK`, `AAB_SIGNING_OK`, R2 parity for 225 objects, `AAB_SIZE_OK 451684148` (48,315,852 under the 500,000,000-byte ceiling), and `AAB_DONE`.

The AAB wrapper again recorded `AAB_BUILD_MARKER_ABSENT`/exit 8 during Unity batch shutdown, while Unity itself logged `Build Successful`; the fresh artifact, signer, parity, and size gates are independently green. This provenance remains a release limitation.

The fresh APK was installed on the connected Seeker with `adb install -r -d`, preserving data. The device now reports version `2026.09.11.365736`, updated at 18:52, and launches to the title screen with the existing `CONTINUE` path. The final raid-victory settlement, damaged-town recovery, repair/design/re-entry and AI-practice loop still require owner-driven device acceptance; no store publication has occurred.

For the next device pass, `tools/capture-seeker-screen.ps1` captures a valid PNG through `adb exec-out screencap -p` and fails closed unless exactly one online device is present. The Seeker is currently disconnected (`adb devices` returns no online devices), so no gameplay acceptance claim is made from this turn.

## 2026-09-11 - catalog-wide fresh-town building visual gate

Added `FreshTownBuildingVisualRegression` to the registered `DataRegression` battery. It enumerates the Addressables-backed structure catalog rather than a hand-maintained sample: 35 subjects (all base visuals plus every authored upgrade tier). Each subject is instantiated with the production `StructureFactory` orientation policy, checked for an enabled mesh and non-null shader materials, fit to its authored height, translated through the shared bounds-seat rule, and verified against the ground plane. Coverage includes every tower tier, Ballista, walls, gates, defenses, and support/building rows.

Fresh standalone run: `Builds/fresh-town-building-visual.log` -> `FRESH_TOWN_BUILDING_VISUAL_OK 35`. Fresh full battery: `Builds/fresh-regression-after-building-gate.log` -> `REGRESSION_OK 513/513 suites -- 513 green, 0 red, 0 skipped`; the registered line repeats the 35-subject visual pass. Neutral captures in `docs/ui-evidence/structure-pose-2026-08-22` also show readable meshes/materials for the watchtower L1-L3, Ballista L1-L2, barracks, jeweler, armorer, and gate.

The existing headed traversal harness still emitted no PNG files, and the throwaway starter PlayMode proof did not reach its capture marker, so those are not claimed as rendered player-town screenshots. The catalog-wide gate is the proven regression now; a fresh on-device screenshot remains an owner acceptance step once Seeker is online.
