# F8 Triage — un-acked inbox seq 5011–5039 (29 captures)

**Written:** 2026-09-14. **Read-only lane.** Nothing acked, nothing edited under `logs/`, no code touched.
Every line below cites a seq + the verbatim harvested text, or a screenshot opened this session.

**Device clock proven UTC-5.** `logs/debug/seeker-365962-logcat.txt:7438` reads
`09-11 22:35:23.900 ... [BREAK] flagged` and seq5025's own `utc` is `2026-09-12T03:35:23.9007590Z` — the
same event. So **"Bridged (local) 2026-09-11 22:38" is only when the daemon pulled the batch**; the
device cluster really spans **09-10 20:12 → 09-11 22:36 local** (4 separate boots), not one instant.

---

## 1. Clusters

| # | Cluster | Seqs | n | Kind | Device utc earliest → latest | Verbatim signature (from the capture's `## Message`) | §12 class | Disposition |
|---|---|---|---|---|---|---|---|---|
| A | Addressables INIT handle invalid | 5011, 5013, 5014, 5017, 5021, 5024 | **6** | error | 2026-09-11T01:12:44Z → 2026-09-12T03:31:46Z | `[Flow:StructureAssets] Addressables INIT handle is INVALID after 3.7s: ... The pass now CONTINUES: an invalid handle means the operation is finished and gone, and the locator enumeration below needs no handle at all.` | **log-level-noise** — the text describes its own success path | **Ack as noise.** Then a 1-line ticket to demote this `FlowTrace.Fail` → `Warn`; as `error` it F8-captures on *every* boot. WO-1089 is `IMPLEMENTED — awaiting the resident-structures confirm`; the confirm is below. |
| B | Arcane albedo bound to zero slots | 5015, 5016, 5018, 5022 | 4 | error | 2026-09-12T00:26:14Z → 02:17:44Z | 5015 `[Flow:Structure] 'arcane-tower': forced texture 'Structures/ArcaneTower_Albedo' RESOLVED but bound onto ZERO of 2 material slot(s) — none declares _BaseMap or _MainTex (first shader 'Synty/Generic_Basic')` | **built-but-invisible** (renders colorless) | **Ack as fixed-but-UNCOMMITTED.** Both emitters are patched to the slot oracle — `StructureFactory.cs:470` calls `DependencyClosureTrace.IsAlbedoSlot(property)`, `HubStructureVisualInjector.cs:677` binds "every albedo-classified slot" — and the oracle is code, not comment (`DependencyClosureTrace.cs:231 public static bool IsAlbedoSlot(...)`). **But `git status --short` reads `M` on all four files** (`DependencyClosureTrace.cs`, `TripoMaterialFixer.cs`, `StructureFactory.cs`, `HubStructureVisualInjector.cs`) — working tree, not shipped. `docs/WORKLOG_2026-09-11.md:18` says a post-fix visual capture is **still owed**. |
| C | 180 s no-progress softlock | 5012, 5019, 5020, 5023 | 4 | possible_softlock | 2026-09-11T01:36:07Z → 2026-09-12T02:23:48Z | `No movement or progress for 180s in 'Main_Castle_Overworld'; classification=SOFTLOCK input=True focused=True worldLive=True` | **log-level-noise (detector)** + it carries two *real* visual findings, below | **Hold** — see §4. WO-1237 (`softlock detector fires on afk`) is CLOSED 2026-08-26; this is the same shape and the detector is firing again. |
| D | EMERGENCY pill, raid scene | 5031, 5032, **5038, 5039** | 4 | error | payload utc 2026-09-11T14:21:00Z / 14:23:11Z | `[Flow:Hero]   EMERGENCY pill spawned in scene 'RaidBase_IronBastion' — carried hero not found.` (`HeroControlEnsurer.cs:703`) | **test-harness-noise** | **Ack.** `RaidBase_IronBastion` is loaded directly by editor proof harnesses (`Assets/Editor/OwnedTownMovePlayProof.cs`, `OwnedTownReconstructionProof.cs`, `FinalRaidRouteProof.cs`) — no carried hero exists by construction. 5038/5039 are byte-identical re-emissions of 5031/5032 (same `t`, same `utc`). |
| E | OwnedTownRepairService CS errors | 5033, 5034, 5035, 5036 | 4 | error | payload utc 2026-09-11T14:23:34Z / 14:27:03Z | `OwnedTownRepairService.cs(17,21): error CS0029: Cannot implicitly convert type 'DeNelle.Core.Catalog.ResourceCost' to 'DeNelle.Village.ResourceCost'` | **test-harness-noise / STALE** | **Ack.** Already fixed in the tree: `Assets/_Modules/Village/World/Camps/OwnedTownRepairService.cs:3` is now `using CoreCost = DeNelle.Core.Catalog.ResourceCost;` and every signature uses `CoreCost`; the cited lines no longer hold that code. ⚠ `git status --short` reads **`??`** on this file — it is **untracked**, never committed. |
| F | Injected save failure | 5037 | 1 | error | payload utc 2026-09-11T16:52:25Z | `[Flow:Save] local Save FAILED (provider write) — progress not persisted this frame. IOException: Injected preview save failure` | **test-harness-noise** — "Injected" is a deliberate fault | **Ack.** |

**Clusters D–F share one defect of the harness, not of the game:** all nine desktop captures (5031–5039)
carry payload `utc` in **2026-09-11** while the inbox filed them **2026-09-14 02:45–03:06** on a ~150 s
cadence, and 5037's `## Auto-harvested context` is a *live* `Main_Castle_Overworld` FTUE session
(`[Flow:Tutorial] walk-probe ... guideBody=ALIVE`, `[Flow:Perf] cpuFrame=16.6ms`) that has nothing to do
with its `OwnedTown_IronBastion` payload. That is **the daemon re-scanning an old `break-log.jsonl` and
re-emitting already-handled rows** when a fresh Play session re-baselined it (§14). Recommend one harness
ticket. *(This refines the brief: a Codex run may have triggered the replay; it did not produce the events.)*

---

## 2. The six owner FLAG presses — seq 5025–5030

**The screenshot mapping is PROVEN, not "candidates".** Capture-to-capture deltas are
**1.5 / 13.9 / 3.4 / 13.2 / 6.9 s**; the on-screen "Next wave in" clock across
`flag_20260912-033141_00…_05.png` reads **14m56 / 14m55 / 14m42 / 14m38 / 14m25 / 14m18** — deltas
**1 / 13 / 4 / 13 / 7 s**. One-to-one, in order: `_00`→5025 … `_05`→5030.

**The nearest `[Flow:*]` lines are NOT in the capture files** (device captures carry no harvest block).
They are in `logs/debug/seeker-365962-logcat.txt`, same boot, same clock:

| seq | screenshot | what the player sees | nearest Flow line (logcat, verbatim) | Δ to the flag |
|---|---|---|---|---|
| 5025 | `_00` | Pause/settings card open mid-screen. At its bottom edge **`PAUSE` and `PAIR ALL` overprint each other**, with `Wood 15  Iron 7` painted across the panel — that is the **RE**PAIR ALL button and its price rendering *through/under* the menu. Town otherwise normal, wave 2. | `22:35:19.492 [Flow:Repair] hub repair affordance AVAILABLE + affordable: cost Wood 15  Iron 7, wallet=W211 I146 F186` | flag +4.4 s |
| 5026 | `_01` | Same frame 1.5 s later, same overprint. A green-roofed building sits with a visible **gap under it — floating off the ground**. | `22:35:25.256 [Flow:Repair] tap hit 'Archer Tower' but RepairTarget could not wrap it - no repair prompt.` | flag **+0.127 s** |
| 5027 | `_02` | Menu closed. Empty courtyard, one NPC, a scaffold behind the gate. Ground is the high-frequency green/teal sparkle. | `22:35:39.091 [Flow:Repair] tap hit 'Wall_4' but RepairTarget could not wrap it - no repair prompt.` | flag **+0.161 s** |
| 5028 | `_03` | Camera swung NW. A green/timber building is **cut off mid-wall by the ground plane — sunk**; a second building behind it likewise. | `22:35:41.384 [Flow:Repair] tap hit 'ExteriorTerrain' ... no repair prompt.` | flag +1.29 s |
| 5029 | `_04` | A **woodpile/stockpile rack floating above the grass and clipping into the wall**, extending past the frame edge. | `22:35:55.711 [Flow:Repair] tap hit 'Wall_13' but RepairTarget could not wrap it - no repair prompt.` | flag **+0.156 s** |
| 5030 | `_05` | A timber scaffold/frame **tipped ~30° onto its side**, half through the wall, hovering above the ground. | `22:36:02.708 [Flow:Repair] tap hit 'Wall_DoorJamb_R' ... no repair prompt.` | flag **+0.091 s** |

**Best-evidenced reading:**

1. **DEFECT — PROVEN. Walls do not wrap as repair targets.** From 22:32:00 onward the log carries ~25
   `tap hit '<X>' but RepairTarget could not wrap it - no repair prompt` lines (and the emitter is
   `FlowTrace.Throttle(..., 2f)` at `WallRepairController.cs:419`, so **that count is a floor, not a
   total**). The Flow text is its own contradiction: *"RepairTarget covers WallSegment/Gate/Building
   only"* — yet the objects that failed to wrap **are walls**: `Wall_4`, `Wall_13`, `Wall_DoorJamb_R`,
   `Battlement_1`, `Battlement_3`, `Battlement_13`. Also unwrappable: `Archer Tower`, `Iron Mine`,
   `Foundry`, `Stoneyard`, `Jeweler_Gems_Storefront`, `Hero (Blaise)`. **Ticket needed.**
2. **ATTRIBUTION — UNPROVEN, and there is a competing mechanism that is proven.** The four tight
   correlations (0.091–0.161 s, world-tap *before* flag every time) are exactly a touch-down →
   touch-up interval, and `WallRepairController` **has no `EventSystem.IsPointerOverGameObject()`
   guard at all** — `:1441` is a bare `Input.GetMouseButtonDown(0)` and `:43` states *"Tap =
   Input.GetMouseButtonDown(0) OR a began touch."* So **pressing the on-screen FLAG button also
   raycasts the world behind it**, and the FLAG button sits left-of-centre where `_02`/`_04`/`_05`
   show wall. The two loose flags (5025 at +4.4 s, 5028 at +1.29 s) do not rescue the strong reading.
   Both stories fit the same bytes; **do not write "she flagged the dead wall taps" as fact.**
3. **NEW TICKET this raises — the FLAG button does not block the world raycast.** If confirmed it
   corrupts the tap context of *every* future flag capture, which is the lane's own evidence supply.
   Worth the lead's attention ahead of the other two.
4. **(strong) The Repair-All affordance collides with the pause menu.** `_00`/`_01` show it overprinting
   `PAUSE`. It *works* — `22:35:31.609 [Flow:Repair] RepairAll: repaired 'Archer Tower' (dmg 0.04 -> post-fix 0.00) for Wood 15  Iron 7` — but the player cannot read it. **Ticket needed.**
5. **UNPROVEN:** whether the sunk/floating/tilted structures in `_02`–`_05` are what she meant, or just
   what the camera happened to hold. The flag carries no free text. The placement defects are real and
   visible in four frames (nearest precedent: WO-1055, archer tower pitched on its side, CLOSED
   2026-08-22) — but attributing the *press* to them is a guess. **Do not act on it without her word.**
6. **UNPROVEN (ambient, present in all six frames):** the grass plane's near-neon shimmer, which WO-1218
   closed as owner-PASS on the 08-27 APK; and a `+4` gain indicator frozen at top-right in every frame.

---

## 3. Proposed ack order (oldest first)

1. **Ack now, in this order:** 5011, 5013, 5014, 5017, 5021, 5024 (cluster A — noise; mint the demote
   ticket first so the signal isn't lost), then 5015, 5016, 5018, 5022 (cluster B — fix in tree, owed a
   visual proof), then 5031–5039 (clusters D/E/F — harness replay of a 09-11 log).
2. **DO NOT ack until the owner confirms the reading: 5025, 5026, 5027, 5028, 5029, 5030.** Her six
   presses are the only un-replaceable evidence here, and reading 3 above is a guess until she rules.
3. **Ack 5012 and 5019 with cluster A** — 5012's screenshot (`break_01`, opened) shows a healthy,
   fully-textured town with the hero idle at the tree and the wave clock running; 5019 has no
   screenshot at all. Both are the detector firing on a stationary player.
   **DO NOT ack 5020 and 5023 yet** — their screenshots carry an unreported visual defect (§4) that
   should have a ticket before the capture is closed.

---

## 4. Contradictions with the briefing context

- **Cluster A is 6 captures, not 7.** 5011, 5013, 5014, 5017, 5021, 5024. No seventh exists in 5011–5039.
- **The four `possible_softlock` captures (5012, 5019, 5020, 5023) were not in the brief at all.** They
  are ~13 % of the batch and they carry the worst screenshots in it. Their PNGs map by the UTC-5 rule:
  5012 → `break_01_possible_softlock.png` (20:36:07), 5020 → `break_03` (19:41:50), 5023 → `break_02`
  (21:23:49); **5019 has no screenshot**. Opened:
  - `break_02` (→5023): a **blank/fresh town** — Lv1, 200 gold. Walls and buildings render as
    **flat white untextured blocks**. This is cluster B's "renders colorless" reaching the whole town.
    **Ticket needed; this is the strongest visual evidence in the batch.** *(UNPROVEN in the same frame:
    both HUD banners are empty of text — but the trunk draws above AND below them, so the banners are in
    front and this is probably the pre-Barracks FTUE state, not a sort-order bug. Several floating
    yellow cubes are present; calling them missing-mesh placeholders would be a guess — the same window
    logs `Poi_NodeAura` / harvest-glow VFX.)*
  - `break_03` (→5020): wall section and a house render **pure white**; hero standing beside an NPC,
    wave clock still running (8m11s). World is live — the "softlock" is a stationary player, not a freeze.
  - `break_01` (→5012): a **different, healthy save** (Grom Lv3, 1172 gold) — buildings fully coloured,
    no white blocks. So the white-town state is save/boot-specific, not universal to this build.
- **The desktop captures' `utc` fields are ALL 2026-09-11**, not only the CS ones — including both
  EMERGENCY-pill pairs and the injected save. And **5038 ≡ 5031, 5039 ≡ 5032 byte-for-byte**. They are
  re-emissions, so "09-14 02:45–03:06" is a filing time, never an event time.
- **Cluster B is three signatures across TWO SEPARATE EMITTERS**, not one: `[Flow:Structure]`
  (5015 `arcane-tower`; 5016 `tower_arcane_spire` / `ArcaneSpire_Albedo` — a *different* structure and
  texture, 3 slots not 2) comes from `StructureFactory.cs:497`, and `[Flow:Hub]
  'ArcaneTower_MagicUpgrades'` (5018, 5022) from `HubStructureVisualInjector.cs:728`. The worklog names
  one fix; **both files are separately patched and both are `M`/uncommitted** — so the fix does cover the
  Hub path, and neither half has shipped.
- **Cluster A's own confirm is sitting in this batch.** seq5024's raw entry reads `"t":6.606288433074951`
  and its `utc` is `2026-09-12T03:31:46Z`; the *same line* appears in the flag session's own logcat at
  `logs/debug/seeker-365962-logcat.txt:566` — `09-11 22:31:46.311 ... E Unity : [Flow:StructureAssets]
  Addressables INIT handle is INVALID after 4.0s`. So 6.6 s into the very boot whose screenshots
  (`flag_*_033141_*`, taken four minutes later at `t≈224–263 s`) show **textured, correctly-skinned walls
  and buildings**. That is a **candidate confirm** for WO-1089's "awaiting the resident-structures
  confirm" — the PO closes it, not this lane.
