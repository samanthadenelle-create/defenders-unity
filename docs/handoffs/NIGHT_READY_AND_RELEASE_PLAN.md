# Night work: six Ready tickets and requested builds

User authorization: complete the six current Ready tickets, perform the proposed regression/storage/collector validation, finish with a passing full regression, then Windows EXE, APK distributed through Firebase, WebGL deployed through Vercel, and AAB. No Google Play/dApp Store submission or git push requested. Use the existing Vercel preview publishing posture unless separately directed to promote production.

Ready inventory read from board parser/current WorkOrder status lines:

- WO-2016: Manage capture regressions and scrolled UI auditor. Validate current product routes, fix capture isolation before any saved-state fixture runs.
- WO-1291: historical Synty re-theme is superseded for castle storefronts by the owner's explicit original-Tripo ruling. Complete restoration/protection evidence against the corrected requirement; do not reapply the rejected art swaps.
- WO-1701: WebGL hero art. Check current prewarm/loader code and the owner-bounced ticket; old sync-failure capture alone does not prove a current RCA.
- WO-1703: textured raid floors reach actual walls; saved-scene and rendered proof.
- WO-1704: wall/gate continuity including inner keep gate and usable navigation; measured builder plus rendered proof.
- WO-1705: highest-tier 3-star capture, durable owned town, repair/design/reentry FTUE, AI practice without wager/reward mutation.

Root owns Unity, package resolution, captures, integration, board generation and publication. Delegated code lanes are file-disjoint and never run Unity. Freeze all C# edits before each Unity invocation. Existing uncommitted owner work is preserved.

Additional validation: storage production dimensions and comparison to approved saved pallets (owner approved the exact saved pallets); collector ownership/rekey/teardown scenarios with isolated persistence; corrected footprint regression plus castle guardrails.

WebGL investigation: the installed editor contains UnityEngine.WebGLModule.dll and the synthetic compile diagnostic asks for a built-in Web GL package. An attempted com.unity.modules.webgl dependency FAILED actual package resolution (night-practice-entry-before.log) and was removed. No dependency fix is claimed. Investigate CompileGate reference collection versus actual WebGL build before changing vendor code.

Storage owner ruling this night: match catalog storage to the saved castle pallets. Owner explicitly says the pallets were hand-authored and placed beside each collector, so verify semantic/positional pairing and preserve their approved dimensions and orientation.

Release sequence after implementation freezes: focused proofs/captures -> isolated source/build provenance checkpoint -> full regression on that exact checkpoint, repair until all pass -> Windows player -> Android APK plus Firebase existing testers group -> release WebGL plus Vercel preview -> signed Google Play AAB and size/content checks. Each artifact needs fresh success marker, timestamp, hash, version and appropriate hosted-content verification. Preserve signing identity and the user's actual save. Failed gates remain failures, never relabelled to reach a build.

Ticket implementation can be marked Fixed with actual test-build evidence; owner-only device acceptance remains explicit. Do not mark untested phone acceptance Closed. Final report must distinguish built, distributed, deployed and unverified.

Fresh night evidence so far: storage reference generation and production Skin mesh/material/size parity passed; root opened all three catalog pallet captures. Sword focused and Play capture passed; front, hip, side and rear frames opened. Practice-entry failed-start guard passed after red baseline. Collector actual lifecycle proof passed with owner keys preserved. Full owned-town gameplay Play proof passed including failure rollback and AI outcomes; construction frame opened. API focused tests passed 37/37. Final integrated regression and builds are still pending.
