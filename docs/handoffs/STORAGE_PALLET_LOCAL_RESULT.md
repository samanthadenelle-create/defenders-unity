# Approved storage pallet implementation

Owner direction: match the saved castle pallets, hand-authored beside each collector. The pairing is lumberyard/Wood_Pallet/LumberMill, foundry/Iron_Pallet/IronMine, silo/Stone_Pallet/Stone_Quarry.

The old catalog rendered all three with GenericContainer at approximately 11.493 x 2 x 11.493 metres. Saved body dimensions were respectively 1.430622 x 0.185851 x 1.787905; 1.724285 x 0.300053 x 1.724285; and 1.552036 x 0.300047 x 1.501247 metres. Wood and stone also use different meshes from that generic body.

Unity generated three render-only reference prefabs under Assets/Prefabs/Village/OwnerStorage, retaining each selected rendering subtree and its complete ancestor transform chain, shared meshes and materials. No ancestor geometry or gameplay components were copied. Three unique Structures/OwnerStorage addresses are registered in Structure_Art; matching catalog rows use the measured uniform footprint cap. JSON twins remain identical and the fallback was regenerated by CatalogFallbackGenerator. The saved castle scene was not changed.

Fresh proof: night-storage-build.log STORAGE_REFERENCE_BUILD_OK; night-storage-fallback-gen.log CATALOG_FALLBACK_GEN_OK; night-storage-parity.log STORAGE_PALLET_PARITY_OK. Root opened all three current StorageCatalog PNGs: gray slatted wood reference, tan covered iron reference, brown slatted stone reference, matching the saved models. Baseline inputs/captures are archived under Builds/night-storage-before.

A separate actual creation/upgrade/signed-memory-save/replay fixture reproduced 36 size/emission failures in night-storage-tier-baseline.log; the old tier fallback enlarged and tinted storage at L2-L6. Its first attempt failed at Unity scene setup only (night-storage-tier-before.log), then that harness setup was corrected. Save isolation passed with three actual writes and the original slot unchanged. StructureTierVisual now identifies catalog storage via TownBankCapacity.IsStorageContainer, restores the captured body scale, and avoids the tier multiplier/emission for those bodies. Fresh night-storage-tier-after.log reports STORAGE_PLACEMENT_PROOF_OK and STORAGE_PLACEMENT_SAVE_ISOLATION_OK with three writes: actual L2-L6 upgrades, L6 replay and non-storage control passed; main save unchanged.

The subsequent real Play lifecycle exposed a separate fill presentation defect. `night-storage-fill-after-setup.log` passed the measurement harness, but the six baseline PNGs showed all fourteen props floating beside an empty pallet. The obsolete GenericContainer approach-edge seat used placement-footprint/collector-slot constants rather than the approved body. Baseline captures and measurements are preserved in `Builds/night-storage-fill-before`. The initial `night-storage-fill.log` failed fixture setup only: the simulated completed layout lacked the `MarkEverBuilt` existence co-gate required by TownBankCapacity. The fixture now records that completed-build identity and retains the real slot assertion.

StorageStackView now measures the pallet rendering bounds in placed-local space before creating fill, measures the existing prop mesh bounds, and fits a compact two-by-two stack within 90% of the deck. Mesh bottoms touch the deck or the layer below; the last two props are centred over the supporting third layer. The original body and prop meshes/materials, resources, capacity, 0/2/5/9/14 visible counts and hysteresis are unchanged. Stored full scales and homes prevent hide/show animation from compounding the temporary 0.6 scale. The old outside-GenericContainer static regression now checks deck fit and off-centre mesh-pivot contact.

Fresh `Builds/night-storage-fill-seated.log:3999` reports `STORAGE_FILL_PLAY_PROOF_OK`: actual SpawnForLayout -> PlacedStructure.Start -> StorageStackView.Start, three empty/full/empty/full cases, nine nonblank captures, unchanged body bounds/meshes/materials/visibility, six in-memory save writes, and original main save unchanged through Play exit. Root opened all three full captures and confirmed seated fill with the approved bodies preserved. Outputs are `Builds/StoragePalletFillPlayProof`; each full/refill pair has identical SHA256 bytes:

| Storage | Full fill size (metres) | Full/refill SHA256 |
| --- | --- | --- |
| lumberyard | 0.859848 x 1.748272 x 1.609116 | BF9DCA4289A3B55177B5052D8E43D731DC0BF5FF36B70782BBCB36060C05CE65 |
| foundry | 0.775955 x 0.969976 x 1.551849 | C3596DCC253F76AF65A04A212DCCE732DC9E6280135522EBAFD2D7026A0A7CD4 |
| silo | 1.396835 x 2.810573 x 1.001968 | 1127A36C2C85E393BAF2F669CF2320DA538970AC92DB5CEE7FCA97B938E1D059 |

All three measured fill-bottom minus pallet-top offsets are 0.000000 m at logged precision. The independent support oracle uses actual renderer bounds: every footprint lies within the deck and each elevated prop's rectangle has at least 99% contact support beneath it, with 2 mm vertical tolerance. This is bounds-level geometric support in yaw-zero fixtures, not triangle-level contact or a physics stability simulation. AABB intersection booleans alone are not the verdict: touching bounds can report either result at floating-point precision. No cold CDN/player-platform proof is claimed. Final combined regression and release validation remain root-owned.

The passing run also contains Unity internal LifecycleManagement NullReferenceExceptions; it is not an exception-free log. Setup exceptions occur at lines 2482/2501 and 2774/2793, before the first capture, in OrderedAssemblyList.TopologicalSortRecursive. Shutdown reports no active lifecycle scope and ExitScope NRE after the pass marker (4006 onward). The shutdown stack includes the fixture's synchronous EditorApplication.Exit inside AfterPlay, but the same internal setup/shutdown pattern exists in the earlier Knight proof (`night-sword-seat-after.log:564,1925`) and the baseline fill proof. Thus the evidence does not establish this fixture's AfterPlay exit as the original cause. No editor/package or lifecycle code change was made. Deferring batch Exit by one EditorApplication.delayCall after EnteredEditMode would be a bounded test of callback reentrancy if root elects to investigate; it cannot be represented as a fix for the earlier assembly-sort failure without a fresh run.
