# WO-1880 RESULT

Implemented 2026-09-19. `WaveManager.StartWave` tracks `wave_started` with `waveId` when phase becomes Active. Funnel view reports `waves.started_players` / `completed_players`. Zero until a build with this emit is in the window.
